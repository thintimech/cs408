import { Lesson } from "@/types";

export const busIoLessons: Lesson[] = [
  {
    id: "bus",
    title: "总线",
    brief: "掌握总线分类、性能指标、仲裁方式和定时方式",
    analogy: "总线像城市的公共道路——所有车辆（部件）共用一条路（总线），但同一时刻只能一辆车通过（总线独占）。仲裁就是交通信号灯（决定谁先走），定时就是限速规则（什么时候数据有效）。",
    prerequisites: ["计算机系统概述", "数字逻辑基础"],
    commonMistakes: [
      "总线带宽 ≠ 总线频率——带宽 = 频率 × 数据线宽度 / 每次传输周期数",
      "同步总线不是没有时钟——恰恰相反，同步总线靠统一时钟协调，异步总线靠握手信号",
      "总线仲裁的链式查询优先级最高的离仲裁器最近——不是编号最小的",
      "PCI总线是并行总线——PCIe虽然名字里有PCI但是串行总线",
    ],
    memoryAids: [
      "总线分类：数据总线(传数据)、地址总线(传地址)、控制总线(传信号)",
      "仲裁三种：链式查询(简单不公平)、计数器定时(较公平)、独立请求(最快最贵)",
      "定时两种：同步(统一时钟，简单快)、异步(握手信号，灵活慢)",
      "总线带宽 = 总线宽度 × 总线频率 / 每次传输的时钟周期数",
    ],
    sections: [
      {
        id: "bus-motivation",
        title: "总线的作用",
        type: "motivation",
        content: `计算机各部件之间需要传输数据、地址和控制信号。如果每两个部件之间都有专用连线，连线数量会随部件数量爆炸增长。

总线提供了一种共享的传输通道，用较少的连线连接多个部件。但共享带来了竞争问题，需要仲裁和定时机制来协调。`,
      },
      {
        id: "bus-classification",
        title: "总线分类与性能",
        type: "concept",
        content: `## 按功能分类

| 总线类型 | 传输内容 | 方向 |
|----------|----------|------|
| 数据总线(DB) | 数据 | 双向 |
| 地址总线(AB) | 地址 | 单向(CPU发出) |
| 控制总线(CB) | 控制信号 | 双向(各种控制) |

## 按层次分类

- **片内总线**：CPU内部各寄存器之间
- **系统总线**：CPU、主存、IO接口之间
- **通信总线**：计算机之间或计算机与外设之间

## 性能指标

| 指标 | 含义 | 公式 |
|------|------|------|
| 总线宽度 | 数据线根数 | 如32位、64位 |
| 总线频率 | 时钟频率 | 如100MHz |
| 总线带宽 | 最大传输速率 | 带宽 = 宽度 x 频率 |
| 总线复用 | 地址/数据线共用 | 节省引脚 |

示例：64位总线，频率200MHz
带宽 = 64bit x 200MHz = 12800Mbit/s = 1600MB/s`,
      },
      {
        id: "bus-arbitration",
        title: "总线仲裁",
        type: "detail",
        content: `多个设备同时请求使用总线时，需要仲裁决定谁先使用。

## 集中仲裁

### 链式查询（菊花链）
- 总线授权信号串行传递
- 离仲裁器近的优先级高
- 优点：简单，扩展容易
- 缺点：优先级固定，远端设备可能饿死

### 计数器定时查询
- 仲裁器用计数器轮询
- 计数可从0开始（固定优先级）或从上次停止处开始（公平）
- 需要 ceil(log2(n)) 根设备地址线

### 独立请求
- 每个设备有独立的请求线和授权线
- 优点：响应快，优先级灵活
- 缺点：线数多（2n根）

## 分布仲裁

无集中仲裁器，各设备自行协商。每个设备有优先级编号，通过总线竞争确定胜者。`,
      },
      {
        id: "bus-timing",
        title: "总线定时",
        type: "comparison",
        content: `## 三种定时方式对比

| 方式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| 同步定时 | 统一时钟控制 | 简单，速度快 | 必须按最慢设备设计 |
| 异步定时 | 握手信号(请求/应答) | 适应不同速度设备 | 复杂，有同步开销 |
| 半同步 | 时钟+等待信号 | 折中方案 | 仍需时钟 |

## 异步定时的握手方式

### 不互锁
- 主设备发请求后不等应答就撤销
- 最快但不可靠

### 半互锁
- 主设备等从设备应答后才撤销请求
- 从设备不等主设备撤销就撤销应答

### 全互锁
- 双方都等对方响应后才撤销
- 最可靠但最慢`,
      },
    ],
    exercises: [
      {
        id: "bus-ex1",
        title: "总线带宽计算",
        description: "某同步总线的时钟频率为66MHz，总线宽度为32位，每个总线传输周期需要2个时钟周期。求：(1)总线带宽；(2)若采用突发传输(burst)，一次传输4个数据，地址只传一次（1个周期），求突发传输的有效带宽。",
        difficulty: "medium",
        hints: [
          "每次传输需要2个时钟周期",
          "带宽 = 数据量 / 时间",
          "突发传输：1个地址周期 + 4个数据周期",
        ],
        referenceSolution: `(1) 普通传输带宽：
   每次传输：32bit = 4B
   传输周期：2个时钟 = 2/66MHz = 30.3ns
   带宽 = 4B / 30.3ns = 132MB/s
   或：带宽 = 32bit x (66MHz/2) = 1056Mbit/s = 132MB/s

(2) 突发传输：
   一次突发传输4个数据：
   时间 = 1个地址周期 + 4个数据周期 = (1+4) x 2 = 10个时钟
   但突发传输中数据可以每周期传一个：
   时间 = 2(地址) + 4x1(数据) = 6个时钟周期
   数据量 = 4 x 4B = 16B
   有效带宽 = 16B / (6/66MHz) = 16B / 90.9ns = 176MB/s`,
      },
    ],
    keyTakeaways: [
      "总线带宽 = 总线宽度 x 工作频率（注意传输周期可能多于1个时钟）",
      "集中仲裁三种：链式查询(简单)、计数器(较公平)、独立请求(最快)",
      "同步定时简单但受限于最慢设备，异步定时灵活但复杂",
      "突发传输通过连续传送多个数据提高有效带宽",
    ],
    relatedLessons: ["io-system"],
  },
  // PLACEHOLDER_BUS_2
  {
    id: "io-system",
    title: "IO系统",
    brief: "掌握程序查询/中断/DMA三种IO方式及中断处理过程",
    analogy: "三种IO方式像三种等快递的策略——程序查询是你每隔5分钟看一次门口（CPU轮询，效率最低），中断是快递到了门铃响你再去开门（CPU可以干别的），DMA是快递员有钥匙自己放进来只在全部送完时通知你一声（CPU几乎不参与传输过程）。",
    prerequisites: ["总线", "CPU基本结构"],
    commonMistakes: [
      "中断响应不是随时都能发生——CPU在每条指令执行结束时检查中断请求",
      "中断向量不是中断服务程序——中断向量是中断服务程序的入口地址",
      "DMA请求优先级高于中断请求——因为DMA不及时响应会丢失数据",
      "保存现场是保存CPU寄存器内容——不是保存内存中的数据",
    ],
    memoryAids: [
      "中断处理过程：关中断→保存断点→识别中断源→保存现场→开中断→执行中断服务→关中断→恢复现场→开中断→中断返回",
      "DMA vs 中断：DMA以块为单位传送，中断以字节/字为单位",
      "中断向量表：中断类型号→中断向量（入口地址）的映射表",
    ],
    sections: [
      {
        id: "io-motivation",
        title: "IO控制方式的演进",
        type: "motivation",
        content: `CPU速度远快于IO设备。如果CPU一直等待IO完成（程序查询），效率极低。

从程序查询到中断再到DMA，IO控制方式的演进目标就是：让CPU尽可能少地参与IO操作，把时间用在计算上。`,
      },
      {
        id: "io-methods",
        title: "三种IO方式对比",
        type: "comparison",
        content: `| 方式 | CPU参与度 | 数据传送单位 | 优点 | 缺点 |
|------|-----------|-------------|------|------|
| 程序查询 | 全程等待 | 字/字节 | 简单 | CPU利用率极低 |
| 中断方式 | 仅在中断时 | 字/字节 | CPU可做其他事 | 每个字都要中断，频繁中断开销大 |
| DMA | 仅在开始和结束 | 数据块 | CPU几乎不参与 | 需要DMA控制器硬件 |

## 程序查询方式

CPU不断查询IO设备状态，直到设备就绪才传送数据。
\`\`\`
循环：读取设备状态寄存器
      if 设备就绪 then 传送数据
      else 继续循环
\`\`\`
CPU在等待期间完全空闲，效率最低。

## 中断方式

IO设备就绪后主动向CPU发中断请求，CPU响应中断后传送数据。

优点：CPU在等待IO期间可以执行其他程序。
缺点：每传送一个字就要中断一次，高速设备频繁中断开销大。

## DMA方式

DMA控制器直接控制内存与IO设备之间的数据传送，无需CPU逐字干预。

CPU只需：
1. 设置DMA参数（起始地址、传送字数、传送方向）
2. 启动DMA
3. DMA完成后收到中断通知`,
      },
      {
        id: "io-interrupt",
        title: "中断处理过程",
        type: "detail",
        content: `## 中断处理的完整流程

### 1. 中断请求
设备通过中断请求线向CPU发出请求。

### 2. 中断判优
多个中断同时请求时，按优先级选择：
- 硬件排队器（链式或编码器）
- 软件查询

### 3. 中断响应
CPU在当前指令执行完毕后检查中断：
- 关中断（防止新中断打断）
- 保存断点（PC -> 栈）
- 转入中断服务程序（通过中断向量）

### 4. 中断服务
- 保护现场（通用寄存器压栈）
- 执行中断处理
- 恢复现场（出栈恢复寄存器）

### 5. 中断返回
- 开中断
- 恢复PC（从栈中弹出断点地址）

## 多重中断（中断嵌套）

高优先级中断可以打断低优先级中断的处理。
条件：在中断服务程序中开中断，且新中断优先级更高。`,
      },
      {
        id: "io-dma",
        title: "DMA传送方式",
        type: "detail",
        content: `## DMA控制器的组成

- 主存地址寄存器（AR）：当前传送的内存地址
- 字计数器（WC）：剩余传送字数
- 数据缓冲寄存器（BR）：暂存数据
- 控制/状态寄存器：控制DMA工作方式

## DMA与CPU共享主存的三种方式

### 1. 停止CPU访存
DMA传送期间，CPU完全不能访问主存。
- 简单，但CPU效率低

### 2. 周期窃取（Cycle Stealing）
DMA每需要传送一个字时，占用一个存取周期。
- CPU和DMA交替使用主存
- CPU可能被延迟1个周期
- 最常用的方式

### 3. 交替分时访问
将存取周期分为两个时间片，CPU和DMA各用一半。
- 不需要总线申请，无冲突
- 要求存储器速度是CPU的2倍

## DMA vs 中断

| 对比 | 中断 | DMA |
|------|------|-----|
| 响应时机 | 指令执行完 | 存取周期结束 |
| 传送单位 | 字/字节 | 数据块 |
| CPU参与 | 程序控制传送 | 仅初始化和结束 |
| 优先级 | DMA优先级高于中断 | - |`,
      },
    ],
    exercises: [
      {
        id: "io-ex1",
        title: "IO方式选择与计算",
        description: "某磁盘转速7200RPM，每个磁道16个扇区，每扇区512字节。CPU时钟频率1GHz，每次中断处理需要500个时钟周期。问：(1)磁盘数据传输率是多少？(2)若采用中断方式每字节中断一次，中断处理占CPU时间的百分比？(3)为什么必须用DMA？",
        difficulty: "hard",
        hints: [
          "转速7200RPM = 120转/秒",
          "每转传输16x512字节",
          "中断频率 = 数据传输率(字节/秒)",
        ],
        referenceSolution: `(1) 磁盘数据传输率：
   转速 = 7200RPM = 120转/秒
   每转数据量 = 16 x 512 = 8192字节 = 8KB
   传输率 = 120 x 8192 = 983040 B/s ≈ 960KB/s

(2) 中断方式（每字节中断一次）：
   中断频率 = 983040次/秒
   每次中断耗时 = 500周期 / 1GHz = 500ns
   CPU用于中断的时间 = 983040 x 500ns = 0.4915s（每秒）
   占CPU时间 = 49.15%

(3) 中断方式占用CPU近50%的时间，严重影响CPU执行其他任务。
   而且这只是一个磁盘，多个IO设备同时工作时CPU将不堪重负。
   DMA方式下CPU只需在传输开始和结束时介入，中间由DMA控制器
   自动完成数据传送，CPU利用率大幅提高。`,
      },
    ],
    keyTakeaways: [
      "程序查询最简单但CPU空等，中断方式CPU可并行但频繁中断开销大",
      "DMA适合高速块设备，CPU仅在开始/结束时参与",
      "中断响应在指令结束后，DMA响应在存取周期结束后（更及时）",
      "周期窃取是DMA最常用的方式，每次只占一个存取周期",
    ],
    relatedLessons: ["bus"],
  },
];

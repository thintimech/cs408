import { Lesson } from "@/types";

export const sortExternalLessons: Lesson[] = [
  {
    id: "external-sort",
    title: "外部排序",
    brief: "数据量超过内存时的排序策略，核心是减少磁盘IO次数",
    keyTakeaways: [
      "外部排序基本方法：生成初始归并段 + 多路归并",
      "k路归并：每趟将k个有序段合并为1个，归并趟数=⌈log_k(初始段数)⌉",
      "增大k可减少趟数，但k路归并内部比较次数增加",
      "败者树：k路归并时用败者树选最小值，比较次数从k-1降为⌈log₂k⌉",
      "置换-选择排序：生成比内存更长的初始归并段",
      "最佳归并树：类似哈夫曼树，使总IO次数最少",
    ],
    relatedLessons: ["merge-sort"],
    sections: [
      {
        id: "external-motivation",
        title: "为什么需要外部排序",
        type: "motivation",
        content: `## 内存放不下怎么办？

假设有一个 **10GB** 的文件需要排序，但内存只有 **512MB**。

- 快速排序、归并排序等内部排序算法要求数据全部在内存中
- 10GB 数据无法一次性载入内存
- 必须借助**外存（磁盘）**来完成排序

---

## 外存访问的代价

| 操作 | 耗时 |
|------|------|
| 内存访问 | ~100ns |
| 磁盘随机IO | ~10ms |
| 差距 | **10万倍** |

**核心目标**：尽量减少磁盘IO次数（读/写磁盘块的次数），而不是减少比较次数。

---

## 外部排序的基本思路

1. 将大文件分成若干段，每段能放入内存
2. 对每段在内存中排序，写回磁盘（生成**初始归并段**）
3. 对这些有序段进行**多路归并**，最终得到完整有序文件`,
      },
      {
        id: "external-basic-method",
        title: "外部排序的基本方法",
        type: "concept",
        content: `## 两个阶段

### 阶段1：生成初始归并段

1. 将文件分为若干段，每段大小 = 内存工作区大小
2. 依次将每段读入内存
3. 用内部排序算法（如快排）排序
4. 将排好序的段写回磁盘

若文件大小为 F，内存工作区大小为 M，则生成 **r = ⌈F/M⌉** 个初始归并段。

### 阶段2：多路归并

将 r 个有序段通过多趟归并合并为1个有序文件。

---

## k路归并的趟数

每趟归并将 k 个有序段合并为 1 个，则：

\`\`\`
归并趟数 S = ⌈log_k(r)⌉
\`\`\`

其中 r 为初始归并段数，k 为归并路数。

---

## 举例

100个初始归并段，5路归并：

\`\`\`
S = ⌈log₅(100)⌉ = ⌈2.86⌉ = 3 趟
\`\`\`

每趟归并需要读写所有数据各一次，设每个归并段占 b 个磁盘块：
- 每趟IO次数 = 2 * r * b（读一遍 + 写一遍）
- 总IO次数 = S * 2 * r * b = 3 * 2 * 100 * b

---

## 减少IO的两个方向

| 策略 | 方法 | 效果 |
|------|------|------|
| 减少归并趟数 S | 增大归并路数 k | S = ⌈log_k(r)⌉ 减小 |
| 减少初始段数 r | 生成更长的初始归并段 | S = ⌈log_k(r)⌉ 减小 |`,
      },
      {
        id: "external-loser-tree",
        title: "【算法】多路归并与败者树",
        type: "detail",
        content: `## k路归并的问题

k路归并每次需要从 k 个段的当前元素中选出最小值：

- **朴素方法**：逐一比较，每次选最小值需要 **k-1 次比较**
- 总比较次数：(n-1) * (k-1)，其中 n 为总记录数
- k 增大时，虽然趟数减少，但每趟内部比较次数增加

---

## 败者树（Loser Tree）

败者树是一种**树形选择**结构，能将每次选最小值的比较次数从 k-1 降为 **⌈log₂k⌉**。

### 结构说明

- 败者树是一棵**完全二叉树**
- 有 k 个叶子结点，分别对应 k 个归并段的当前元素
- 每个**内部结点**记录的是该结点两个孩子比较后的**"败者"编号**（较大者）
- **"胜者"**（较小者）继续向上比较
- 根结点的上方额外设一个结点，记录最终的**冠军**（全局最小值）

### 工作过程

\`\`\`
初始：k个段各取第一个元素，构建败者树

每次输出最小值后：
1. 从最小值所在的段取下一个元素
2. 该元素从叶子位置向上调整
3. 每层只需与"败者"比较一次
4. 经过 ⌈log₂k⌉ 层到达根部，确定新的最小值
\`\`\`

---

## 性能对比

| 方法 | 每次选最小值的比较次数 | k=8时 |
|------|----------------------|-------|
| 朴素比较 | k-1 | 7次 |
| 败者树 | ⌈log₂k⌉ | 3次 |

**结论**：使用败者树后，增大 k 不会显著增加内部比较次数，可以放心增大归并路数来减少IO趟数。

---

## 败者树 vs 胜者树

| 特性 | 败者树 | 胜者树 |
|------|--------|--------|
| 内部结点记录 | 败者（较大者） | 胜者（较小者） |
| 调整时比较对象 | 与父结点记录的败者比较 | 需要与兄弟结点比较 |
| 实现便利性 | **更方便**（直接与父结点比） | 需要额外访问兄弟 |

败者树在实现上更简洁，因此实际中更常用。`,
      },
      {
        id: "external-replacement-selection",
        title: "置换-选择排序",
        type: "concept",
        content: `## 动机

基本方法生成的初始归并段长度 = 内存工作区大小 M。能否生成**更长**的归并段？

段越长 -> 段数 r 越少 -> 归并趟数 S = ⌈log_k(r)⌉ 越少 -> IO次数越少。

---

## 置换-选择排序（Replacement-Selection Sort）

利用一个大小为 M 的**最小堆**（优先队列），可以生成平均长度为 **2M** 的初始归并段。

### 基本过程

1. 从文件读入 M 个记录，建立最小堆
2. 重复以下步骤直到堆为空：
   - 输出堆顶（最小值）到当前归并段
   - 从文件读入下一个记录 R
   - 若 R >= 刚输出的值：R 可以加入当前段，放入堆中调整
   - 若 R < 刚输出的值：R 不能加入当前段（会破坏有序性），标记为"下一段"的元素
3. 当堆中所有元素都被标记为"下一段"时，当前段结束，开始新段

---

## 为什么段长可以超过 M？

- 只要新读入的记录 >= 当前段最后输出的值，就能继续追加到当前段
- 对于随机数据，平均约有一半的新记录满足条件
- 数学期望：平均段长 = **2M**

---

## 示例

内存工作区 M=3，输入序列：[4, 2, 7, 1, 5, 3, 8, 6]

| 步骤 | 输出 | 读入 | 说明 | 堆(步骤后) |
|------|------|------|------|------------|
| 初始 | - | - | 建堆 | {2,4,7} |
| 1 | 2 | 1 | 1<2，标记为下一段 | {4,7} |
| 2 | 4 | 5 | 5>=4，加入当前段 | {5,7} |
| 3 | 5 | 3 | 3<5，标记为下一段 | {7} |
| 4 | 7 | 8 | 8>=7，加入当前段 | {8} |
| 5 | 8 | 6 | 6<8，标记为下一段。堆空，段结束 | {} |

第一段输出：[2, 4, 5, 7, 8]（长度5 > M=3）`,
      },
      {
        id: "external-optimal-merge",
        title: "最佳归并树",
        type: "concept",
        content: `## 问题引入

若各初始归并段的**长度不等**，归并顺序会影响总IO次数。

短段被多次参与归并 -> 被反复读写 -> IO次数增加。

---

## 最佳归并树（类似哈夫曼树）

**思想**：让短段尽早归并（放在树的底层），长段尽晚归并（放在树的顶层），使得**总IO次数最少**。

这与哈夫曼树的思想完全一致：
- 每个初始归并段 = 一个叶子结点
- 段的长度（记录数）= 叶子的权值
- 归并树的**带权路径长度（WPL）**= 总IO的磁盘块数

---

## 构造方法

对于 k 路归并：

1. 将所有初始归并段按长度排序
2. 每次选 k 个最短的段进行归并
3. 归并结果作为新段，重新参与选择
4. 重复直到只剩1个段

---

## 补虚段问题

k路最佳归并树是一棵**严格k叉树**（每个非叶结点恰好有k个孩子）。

设初始归并段数为 r，则需要满足：

\`\`\`
(r - 1) % (k - 1) == 0
\`\`\`

若不满足，需要补充**虚段**（长度为0的段），使得条件成立。

补虚段数 = (k - 1 - (r - 1) % (k - 1)) % (k - 1)

---

## 举例

6个初始归并段，长度分别为：[2, 3, 5, 7, 9, 11]，进行3路归并。

检查：(6-1) % (3-1) = 5 % 2 = 1 != 0

需要补 (2 - 1) % 2 = 1 个虚段（长度为0）。

段序列变为：[0, 2, 3, 5, 7, 9, 11]

构造过程：
1. 选最小的3个：0, 2, 3 -> 归并得到长度5的段
2. 当前段：[5, 5, 7, 9, 11]
3. 选最小的3个：5, 5, 7 -> 归并得到长度17的段
4. 当前段：[9, 11, 17]
5. 选最小的3个：9, 11, 17 -> 归并得到长度37的段

总IO = (0+2+3)*2 + (5+5+7)*2 + (9+11+17)*2 = 10 + 34 + 74 = 118 个磁盘块读写

（乘以2是因为每个段参与归并时需要读一次、写一次）`,
      },
    ],
    exercises: [
      {
        id: "external-ex1",
        title: "多路归并趟数与IO计算",
        description: `某文件有4500个记录，内存工作区可容纳750个记录。采用置换-选择排序生成初始归并段后，得到6个长度分别为 [800, 900, 700, 1200, 500, 400] 的归并段。

1. 若采用3路归并，计算归并趟数
2. 若每个记录占1个磁盘块，计算不使用最佳归并树时的总磁盘IO次数（假设按原始顺序每3个一组归并）
3. 使用最佳归并树（3路）时的总磁盘IO次数是多少？需要补虚段吗？`,
        difficulty: "medium",
        hints: [
          "归并趟数 S = ⌈log_k(r)⌉，其中 r=6, k=3",
          "每趟归并需要读写所有记录各一次，IO = 2 * 总记录数 * 趟数",
          "最佳归并树：检查 (r-1)%(k-1) 是否为0，若不为0需补虚段",
          "最佳归并树的总IO = 2 * WPL（带权路径长度）",
        ],
        referenceSolution: `1. 归并趟数：
   S = ⌈log₃(6)⌉ = ⌈1.63⌉ = 2 趟

2. 不使用最佳归并树（按顺序归并）：
   第1趟：[800,900,700] -> 2400，[1200,500,400] -> 2100
   第2趟：[2400, 2100] -> 4500（只有2路，不满3路）

   总IO = 第1趟读写 + 第2趟读写
        = 2*4500 + 2*4500 = 18000 次磁盘IO

3. 最佳归并树（3路）：
   检查：(6-1) % (3-1) = 5 % 2 = 1 != 0
   需要补 1 个虚段（长度0）。

   段序列：[0, 400, 500, 700, 800, 900, 1200]

   第1轮选最小3个：0, 400, 500 -> 900
   当前：[700, 800, 900, 900, 1200]
   第2轮选最小3个：700, 800, 900 -> 2400
   当前：[900, 1200, 2400]
   第3轮：900, 1200, 2400 -> 4500

   WPL = (0+400+500)*2 + (700+800+900)*2 + (900+1200+2400)*1
   注意：WPL按路径长度计算
   - 0,400,500 在第3层，路径长度=2
   - 700,800,900 在第2层，路径长度=2

   实际用IO计算：
   - 第1轮IO：2*(0+400+500) = 1800
   - 第2轮IO：2*(700+800+900) = 4800
   - 第3轮IO：2*(900+1200+2400) = 9000
   总IO = 1800 + 4800 + 9000 = 15600 次

   比顺序归并的18000次少了2400次IO。`,
      },
      {
        id: "external-ex2",
        title: "败者树的构建与调整",
        description: `有5个有序归并段，当前各段的首元素分别为：
- 段0: 10
- 段1: 15
- 段2: 8
- 段3: 20
- 段4: 12

1. 画出初始败者树（内部结点记录败者编号，根上方记录冠军编号）
2. 输出当前最小值后，段2的下一个元素为18，画出调整后的败者树
3. 与朴素k-1次比较相比，败者树在此例中每次选最小值节省了多少次比较？`,
        difficulty: "hard",
        hints: [
          "败者树有5个叶子（对应5个段），4个内部结点",
          "构建时从底向上比较，败者（较大者）留在父结点，胜者（较小者）向上",
          "调整时只需从被替换的叶子向上比较到根，经过⌈log₂5⌉=3层",
          "朴素方法需要k-1=4次比较，败者树需要⌈log₂5⌉=3次比较",
        ],
        referenceSolution: `1. 初始败者树构建：

叶子结点（b0~b4）：10, 15, 8, 20, 12

构建过程（从底向上）：
- b0(10) vs b1(15)：胜者10，败者b1(15)
- b2(8) vs b3(20)：胜者8，败者b3(20)
- b4(12) 单独一组，直接上升
- 10 vs 12：胜者10，败者b4(12)
- 10 vs 8：胜者8，败者b0(10)

败者树（内部结点记录败者编号）：
         [冠军: 2]
            |
         [败者: 0]     <- ls[1]（根）
        /         \\
    [败者: 4]   [败者: 3]   <- ls[2], ls[3]
     /    \\      /    \\
   b0(10) b1(15) b2(8) b3(20) b4(12)

   ls[4]=1(b1是b0和b1比较的败者)

冠军 = 段2（值为8，最小）

2. 输出8后，段2下一个元素为18：

b2 变为 18，从 b2 位置向上调整：
- b2(18) vs ls[3]记录的败者b3(20)：18<20，胜者b2(18)，败者不变b3(20)
- b2(18) vs ls[1]记录的败者b0(10)：18>10，胜者b0(10)，败者更新为b2(18)
- b0(10) vs 冠军位置：更新冠军为 b0

调整后：
         [冠军: 0]
            |
         [败者: 2]     <- ls[1]
        /         \\
    [败者: 4]   [败者: 3]
     /    \\      /    \\
   b0(10) b1(15) b2(18) b3(20) b4(12)

新的最小值 = 段0（值为10）

3. 比较次数对比：
- 朴素方法：k-1 = 5-1 = 4 次比较
- 败者树：⌈log₂5⌉ = 3 次比较
- 每次选最小值节省 1 次比较
- 若总记录数为n，总共节省 n*(4-3) = n 次比较`,
      },
    ],
  },
];

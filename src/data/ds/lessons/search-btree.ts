import { Lesson } from "@/types";

export const searchBtreeLessons: Lesson[] = [
  {
    id: "b-tree",
    title: "B树与B+树",
    brief: "多路平衡查找树，适用于磁盘存储的大规模数据索引",
    keyTakeaways: [
      "m阶B树：每个结点最多m棵子树，最少⌈m/2⌉棵子树（根除外）",
      "B树的查找：多路查找，每个结点内顺序/折半查找",
      "B树插入：可能导致结点分裂（中间关键字上提）",
      "B树删除：可能导致结点合并（借兄弟或合并）",
      "B+树：所有关键字都在叶子结点，叶子结点用链表连接",
      "B+树更适合范围查询和顺序访问",
    ],
    relatedLessons: ["bst-search", "avl-tree"],
    sections: [
      {
        id: "btree-motivation",
        title: "为什么需要B树",
        type: "motivation",
        content: `## 磁盘IO的代价

磁盘的一次IO操作（寻道+旋转+传输）耗时约 **10ms**，而内存访问仅需 **100ns**，差距达 **10万倍**。

当数据量巨大（如数据库索引），无法全部放入内存时，每次查找都可能触发磁盘IO。

---

## 二叉树的局限

| 树类型 | 高度（n=100万） | 最坏IO次数 |
|--------|----------------|-----------|
| BST/AVL | ~20 | 20次 |
| B树(m=200) | ~3 | 3次 |

二叉树即使平衡，高度也是 O(logn)，对于百万级数据高度约20——意味着最坏情况下需要20次磁盘IO。

---

## 多路查找树的思想

**核心思路**：增加每个结点的分支数（从2路变为m路），大幅降低树的高度。

- 每个结点存储多个关键字，对应多个子树指针
- 一次IO读取一个结点（一个磁盘块），获得多个关键字信息
- 树的高度从 O(logn) 降为 O(log_m(n))，IO次数大幅减少`,
      },
      {
        id: "btree-concept",
        title: "B树的定义与性质",
        type: "concept",
        content: `## m阶B树的定义

一棵m阶B树（m >= 3）或者是空树，或者满足以下性质：

1. **每个结点最多有 m 棵子树**（即最多 m-1 个关键字）
2. **根结点**：若非叶子，则至少有 **2 棵子树**
3. **非根非叶结点**：至少有 **⌈m/2⌉ 棵子树**（即至少 ⌈m/2⌉-1 个关键字）
4. **所有叶子结点在同一层**（B树是绝对平衡的）
5. 结点内关键字**有序排列**：K1 < K2 < ... < Kn

---

## 结点结构

一个含 n 个关键字的结点结构：

\`\`\`
| P0 | K1 | P1 | K2 | P2 | ... | Kn | Pn |
\`\`\`

- Ki：第 i 个关键字
- Pi：第 i 个子树指针，指向关键字范围在 (Ki, Ki+1) 之间的子树
- P0 指向关键字全部小于 K1 的子树

---

## 高度与关键字数的关系

含 N 个关键字的 m 阶B树，高度 h 满足：

\`\`\`
log_m(N+1) <= h <= log_{⌈m/2⌉}((N+1)/2) + 1
\`\`\`

**考研常用结论**：
- 最小高度：所有结点都满（每个结点 m-1 个关键字）
- 最大高度：每个结点关键字数最少（根2个子树，其余⌈m/2⌉个子树）`,
      },
      {
        id: "btree-search",
        title: "【算法】B树的查找",
        type: "detail",
        content: `## 查找过程

B树的查找是一个**从根到叶**的多路查找过程：

1. 从根结点开始
2. 在当前结点中，对 n 个关键字进行**顺序查找或折半查找**
3. 若找到 Ki = key，查找成功
4. 若 Ki < key < Ki+1，沿指针 Pi 下降到子结点
5. 若到达叶子结点仍未找到，查找失败

---

## 伪代码

\`\`\`c
Result SearchBTree(BTree T, int key) {
    BTree p = T;
    BTree parent = NULL;
    while (p) {
        // 在结点 p 中查找 key
        int i = 0;
        while (i < p->n && key > p->keys[i])
            i++;
        if (i < p->n && key == p->keys[i])
            return (p, i, FOUND);  // 查找成功
        // 未找到，沿子树指针下降
        parent = p;
        p = p->children[i];
    }
    return (parent, -1, NOT_FOUND);  // 查找失败
}
\`\`\`

---

## 性能分析

- 每访问一个结点 = 一次磁盘IO
- 结点内查找在内存中完成，代价可忽略
- **查找的IO次数 = 查找路径长度 = 树的高度 h**`,
      },
      {
        id: "btree-insert-walkthrough",
        title: "B树插入过程演示",
        type: "walkthrough",
        content: `3阶B树（2-3树）：每个结点最多2个关键字，最少1个。依次插入 [5, 8, 1, 3, 2, 7, 6, 4]。`,
        steps: [
          {
            description: "插入5：创建根结点[5]",
            state: {
              tree: { val: "5" },
              rotation: "无需分裂",
              balanceFactors: "根结点1个关键字，合法",
            },
          },
          {
            description: "插入8：8>5，插入根结点右侧，变为[5,8]",
            state: {
              tree: { val: "5,8" },
              rotation: "无需分裂",
              balanceFactors: "根结点2个关键字（上限m-1=2），合法",
            },
          },
          {
            description: "插入1：1<5，插入后变为[1,5,8]，3个关键字超出上限！需要分裂",
            state: {
              tree: { val: "1,5,8" },
              rotation: "需要分裂！关键字数=3 > m-1=2",
              balanceFactors: "中间关键字5上提为新根",
              highlight: "5",
            },
          },
          {
            description: "分裂：5上提为根，左[1]右[8]",
            state: {
              tree: { val: "5", left: { val: "1" }, right: { val: "8" } },
              rotation: "分裂完成",
              balanceFactors: "所有结点关键字数合法",
            },
          },
          {
            description: "插入3：3<5进入左子结点[1]，插入后[1,3]，合法",
            state: {
              tree: { val: "5", left: { val: "1,3" }, right: { val: "8" } },
              rotation: "无需分裂",
              balanceFactors: "左子结点2个关键字，合法",
              newNode: "1,3",
            },
          },
          {
            description: "插入2：2<5进入[1,3]，插入后[1,2,3]超出上限！分裂：2上提到父结点",
            state: {
              tree: { val: "2,5", left: { val: "1" }, right: { val: "3" } },
              rotation: "分裂：中间关键字2上提到父结点[5]→[2,5]",
              balanceFactors: "分裂产生新的右兄弟[3]，原结点保留[1]",
              highlight: "2,5",
            },
          },
          {
            description: "插入7：7>5进入[8]，插入后[7,8]，合法",
            state: {
              tree: { val: "2,5", left: { val: "1" }, right: { val: "7,8" } },
              rotation: "无需分裂",
              balanceFactors: "注：此处简化展示，实际[2,5]有3个子结点",
            },
          },
          {
            description: "插入6：6>5进入[7,8]，插入后[6,7,8]超出！分裂：7上提→父结点变[2,5,7]也超出！",
            state: {
              tree: { val: "5", left: { val: "2" }, right: { val: "7" } },
              rotation: "连续分裂！7上提后父结点[2,5,7]也超出，5再上提为新根",
              balanceFactors: "树高度+1，最终形成两层结构",
              highlight: "5",
            },
          },
          {
            description: "插入4：4<5→左子树，4>2→进入[3]，插入后[3,4]合法。最终B树构造完成",
            state: {
              tree: { val: "5", left: { val: "2" }, right: { val: "7" } },
              rotation: "无需分裂，插入完成",
              balanceFactors: "叶子层：[1] [3,4] [6] [8]",
            },
          },
        ],
      },
      {
        id: "btree-insert",
        title: "【算法】B树的插入",
        type: "detail",
        content: `## 插入规则

1. **新关键字一定插入到叶子结点**（先查找确定位置）
2. 插入后若结点关键字数 **> m-1**，则该结点需要**分裂**

---

## 分裂操作

当一个结点有 m 个关键字（超出上限 m-1）时：

1. 取中间位置 ⌈m/2⌉ 的关键字 K
2. K **上提到父结点**
3. 原结点分裂为左右两部分：
   - 左部分：K 左边的关键字
   - 右部分：K 右边的关键字

分裂可能向上传播，若根结点分裂，则树高度增加1。

---

## 示例：3阶B树（2-3树）插入 [5, 8, 1, 3, 2, 7, 6, 4]

3阶B树：每个结点最多2个关键字，最少1个关键字。

**第1步：插入 5**

\`\`\`
[5]
\`\`\`

根结点只有1个关键字，合法。

**第2步：插入 8**

\`\`\`
[5, 8]
\`\`\`

根结点2个关键字，合法（上限 m-1=2）。

**第3步：插入 1**

插入后变为 [1, 5, 8]，3个关键字超出上限，需要分裂：
- 中间关键字 5 上提为新根
- 左：[1]，右：[8]

\`\`\`
    [5]
   /   \\
 [1]   [8]
\`\`\`

**第4步：插入 3**

3 < 5，进入左子结点 [1]，插入后变为 [1, 3]，合法。

\`\`\`
    [5]
   /   \\
[1,3]  [8]
\`\`\`

**第5步：插入 2**

2 < 5，进入左子结点 [1, 3]，插入后变为 [1, 2, 3]，超出上限，分裂：
- 中间关键字 2 上提到父结点
- 左：[1]，右：[3]

\`\`\`
   [2, 5]
  /   |   \\
[1]  [3]  [8]
\`\`\`

**第6步：插入 7**

7 > 5，进入右子结点 [8]，插入后变为 [7, 8]，合法。

\`\`\`
   [2, 5]
  /   |   \\
[1]  [3]  [7,8]
\`\`\`

**第7步：插入 6**

6 > 5，进入右子结点 [7, 8]，插入后变为 [6, 7, 8]，超出上限，分裂：
- 中间关键字 7 上提到父结点
- 左：[6]，右：[8]

父结点变为 [2, 5, 7]，也超出上限，继续分裂：
- 中间关键字 5 上提为新根
- 左：[2]，右：[7]

\`\`\`
       [5]
      /   \\
   [2]    [7]
  /   \\  /   \\
[1] [3] [6] [8]
\`\`\`

树高度增加1。

**第8步：插入 4**

4 < 5，进入左子树；4 > 2，进入 [3]，插入后变为 [3, 4]，合法。

\`\`\`
       [5]
      /   \\
   [2]    [7]
  /   \\  /   \\
[1] [3,4] [6] [8]
\`\`\``,
      },
      {
        id: "btree-delete",
        title: "【算法】B树的删除",
        type: "detail",
        content: `## 删除的三种情况

### 情况1：关键字在叶子结点

直接删除。若删除后关键字数 < ⌈m/2⌉-1，则需要调整。

### 情况2：关键字在非叶子结点

用该关键字的**直接前驱**（左子树中最大的）或**直接后继**（右子树中最小的）替代它，然后转化为删除叶子结点中的关键字（情况1）。

### 情况3：删除后结点关键字不足

当叶子结点删除后关键字数 < ⌈m/2⌉-1 时，需要调整：

---

## 调整策略

**策略A：借兄弟**

若左/右兄弟结点的关键字数 > ⌈m/2⌉-1（有富余），则：
1. 父结点中的分隔关键字下移到当前结点
2. 兄弟结点中最接近的关键字上移到父结点

相当于"旋转"操作。

**策略B：合并**

若左右兄弟都没有富余关键字，则：
1. 当前结点 + 父结点中的分隔关键字 + 一个兄弟结点 合并为一个结点
2. 父结点减少一个关键字

合并可能导致父结点关键字不足，向上传播。若根结点的最后一个关键字被借走，树高度减1。

---

## 删除总结

| 情况 | 处理 |
|------|------|
| 在叶子，删后够 | 直接删 |
| 在非叶子 | 前驱/后继替代，转为叶子删除 |
| 删后不够，兄弟有富余 | 借兄弟（旋转） |
| 删后不够，兄弟也不够 | 合并结点 |`,
      },
      {
        id: "btree-vs-bplus",
        title: "B树 vs B+树",
        type: "comparison",
        content: `## B+树的特点

B+树是B树的变体，在数据库系统中更为常用。

---

## 对比表

| 特性 | B树 | B+树 |
|------|-----|------|
| 关键字位置 | 分布在所有结点 | **所有关键字都在叶子结点** |
| 非叶结点作用 | 存储数据+索引 | **仅作为索引**（不存数据） |
| 叶子结点链表 | 无 | **叶子结点用链表相连** |
| 查找路径 | 可能在任意层结束 | **必须到达叶子层** |
| 查找稳定性 | 不稳定（层数不同） | **稳定（每次都到叶子）** |
| 范围查询 | 需要中序遍历整棵树 | **沿叶子链表顺序扫描** |
| 数据库应用 | 较少使用 | **MySQL InnoDB 默认索引** |

---

## B+树的优势

1. **范围查询高效**：找到起始关键字后，沿叶子链表顺序读取即可
2. **查找性能稳定**：每次查找都走到叶子层，路径长度固定
3. **磁盘读写更优**：非叶结点不存数据，同一个磁盘块能容纳更多索引项，树更矮
4. **全表扫描方便**：遍历叶子链表即可获得所有数据的有序序列

---

## 数据库中的应用

\`\`\`
MySQL InnoDB 索引结构（B+树）：

     [非叶结点：仅存索引]
      /        |        \\
  [索引]    [索引]    [索引]
   /  \\     /  \\     /  \\
[数据]->[数据]->[数据]->[数据]->[数据]->[数据]
         叶子结点：存储实际数据，用链表连接
\`\`\`

- **聚簇索引**：叶子结点存储完整行数据
- **非聚簇索引**：叶子结点存储主键值（回表查询）`,
      },
    ],
    exercises: [
      {
        id: "btree-ex1",
        title: "B树的插入与分裂",
        description: `已知一棵5阶B树（每个结点最多4个关键字，最少2个关键字），初始为空。依次插入关键字序列 [10, 20, 30, 40, 50, 60, 70]。

请画出每次分裂后的B树形态，并回答：
1. 第一次分裂发生在插入哪个关键字时？
2. 最终树的高度是多少？
3. 若继续插入 80, 90，是否会再次分裂？`,
        difficulty: "medium",
        hints: [
          "5阶B树：每个结点最多4个关键字，分裂时取第⌈5/2⌉=3个关键字上提",
          "插入前4个关键字 [10,20,30,40] 时根结点刚好满，插入第5个时触发分裂",
          "分裂时中间关键字（第3个）上提为新根",
        ],
        referenceSolution: `插入 10,20,30,40：根=[10,20,30,40]，满但未超出。

插入 50：根变为[10,20,30,40,50]，5个关键字超出上限(m-1=4)，分裂：
- 中间关键字(第3个) 30 上提为新根
- 左：[10,20]，右：[40,50]

       [30]
      /    \\
[10,20]  [40,50]

插入 60：60>30，进入右子结点，变为[40,50,60]，合法。

       [30]
      /    \\
[10,20]  [40,50,60]

插入 70：60>30，进入右子结点，变为[40,50,60,70]，合法（刚好4个）。

       [30]
      /    \\
[10,20]  [40,50,60,70]

最终树高度为2。

继续插入80：右子结点变为[40,50,60,70,80]，超出上限，分裂：
- 中间关键字 60 上提到根
- 左：[40,50]，右：[70,80]

     [30, 60]
    /    |    \\
[10,20] [40,50] [70,80]

插入90：进入[70,80]，变为[70,80,90]，合法，不分裂。`,
      },
      {
        id: "btree-ex2",
        title: "B树与B+树的对比分析",
        description: `某数据库表有100万条记录，磁盘块大小为4KB，每个关键字占8字节，每个指针占4字节。

1. 若使用B树索引，计算m阶B树中m的最大值（一个结点占一个磁盘块）
2. 计算该B树的最小高度（所有结点都满的情况）
3. 解释为什么B+树在此场景下比B树更优`,
        difficulty: "hard",
        hints: [
          "一个结点最多m-1个关键字和m个指针，总大小不超过4KB",
          "列方程：(m-1)*8 + m*4 <= 4096，解出m的最大值",
          "最小高度时所有结点都满，每个结点m-1个关键字，第h层有m^(h-1)个结点",
          "B+树非叶结点不存数据指针，能容纳更多索引项",
        ],
        referenceSolution: `1. 计算m的最大值：
   每个结点：(m-1)个关键字 + m个指针
   空间约束：(m-1)*8 + m*4 <= 4096
   8m - 8 + 4m <= 4096
   12m <= 4104
   m <= 342
   所以 m 最大为 342。

2. 计算最小高度：
   所有结点都满时，每个结点有341个关键字。
   第1层（根）：1个结点，341个关键字
   第2层：342个结点，342*341 = 116622个关键字
   第3层：342^2 = 116964个结点，116964*341 = 39,884,724个关键字

   前2层总关键字：341 + 116622 = 116963 < 100万
   前3层总关键字：116963 + 39884724 > 100万

   所以最小高度为 3。仅需3次磁盘IO即可在百万数据中定位。

3. B+树更优的原因：
   - B+树非叶结点不存储数据记录指针（B树每个关键字附带数据指针）
   - 省去数据指针后，同一个磁盘块能容纳更多关键字，m值更大
   - m更大意味着树更矮，IO次数更少
   - 此外B+树支持高效范围查询（沿叶子链表扫描）`,
      },
    ],
  },
];

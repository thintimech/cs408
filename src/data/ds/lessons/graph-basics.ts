import { Lesson } from "@/types";

export const graphBasicsLessons: Lesson[] = [
  {
    id: "graph-basics",
    title: "图的基本概念与存储结构",
    brief: "图的定义、术语，以及邻接矩阵和邻接表两种存储方式的对比",
    keyTakeaways: [
      "图G=(V,E)，分有向图和无向图",
      "无向图所有顶点度之和=2|E|，有向图入度之和=出度之和=|E|",
      "邻接矩阵：O(V²)空间，判断边O(1)，适合稠密图",
      "邻接表：O(V+E)空间，遍历邻居O(degree)，适合稀疏图",
      "十字链表用于有向图（方便求入度出度），邻接多重表用于无向图（边只存一次）",
      "DFS/BFS时间复杂度：邻接矩阵O(V²)，邻接表O(V+E)",
    ],
    relatedLessons: ["graph-traversal", "prim", "dijkstra"],
    sections: [
      {
        id: "definitions",
        title: "图的定义与术语",
        type: "concept",
        content: `## 图的定义

图 **G = (V, E)** 由两个集合组成：
- **V**（Vertex Set）：顶点的有穷非空集合
- **E**（Edge Set）：边的有穷集合（可以为空）

### 有向图与无向图

| 类型 | 边的表示 | 说明 |
|------|----------|------|
| 无向图 | (u, v) -- 无序对 | (u,v) 和 (v,u) 是同一条边 |
| 有向图 | <u, v> -- 有序对 | <u,v> 表示从 u 到 v 的弧，与 <v,u> 不同 |

### 度、入度、出度

- **度（Degree）**：与顶点 v 相关联的边的数目，记作 TD(v)
- **入度（InDegree）**：有向图中，以 v 为终点的弧的数目，记作 ID(v)
- **出度（OutDegree）**：有向图中，以 v 为起点的弧的数目，记作 OD(v)
- 有向图中：TD(v) = ID(v) + OD(v)

### 重要结论

**无向图**：所有顶点度之和 = 2|E|

> 原因：每条边贡献两个端点各一度

**有向图**：所有顶点入度之和 = 所有顶点出度之和 = |E|

**完全图**：
- 无向完全图（n个顶点）：边数 = n(n-1)/2
- 有向完全图（n个顶点）：弧数 = n(n-1)

### 路径与连通性

| 术语 | 定义 |
|------|------|
| 路径 | 顶点序列 v1, v2, ..., vk，相邻顶点间有边 |
| 简单路径 | 路径中顶点不重复 |
| 回路（环） | 起点和终点相同的路径 |
| 简单回路 | 除起终点外，其余顶点不重复的回路 |
| 连通 | 无向图中两顶点间有路径 |
| 连通图 | 无向图中任意两顶点均连通 |
| 强连通 | 有向图中两顶点互相可达 |
| 强连通图 | 有向图中任意两顶点均强连通 |
| 生成树 | 连通图的极小连通子图，含全部顶点，n-1条边 |`,
      },
      {
        id: "adjacency-matrix",
        title: "邻接矩阵",
        type: "concept",
        content: `## 邻接矩阵的定义

设图 G 有 n 个顶点，邻接矩阵 A 是一个 n×n 的矩阵：

$$A[i][j] = \\begin{cases} 1 & \\text{若 (vi, vj) 或 <vi,vj> 是图中的边} \\\\ 0 & \\text{否则} \\end{cases}$$

**带权图**：A[i][j] 存权值，无边处存 ∞（代码中常用 INT_MAX 或极大值）。

### C 语言结构体

\`\`\`c
#define MaxVertexNum 100
#define INFINITY 65535  /* 表示无穷大（无边） */

typedef char VertexType;
typedef int EdgeType;

typedef struct {
    VertexType Vex[MaxVertexNum];           /* 顶点表 */
    EdgeType Edge[MaxVertexNum][MaxVertexNum]; /* 邻接矩阵（边表） */
    int vexnum, arcnum;                     /* 顶点数和弧数 */
} MGraph;
\`\`\`

### 特点分析

**优点：**
- 判断两顶点间是否有边：O(1)，直接查 A[i][j]
- 求顶点的度：O(n)，遍历一行（无向图）或一行+一列（有向图）
- 实现简单，适合稠密图

**缺点：**
- 空间复杂度 O(n²)，与边数无关，稀疏图浪费严重
- 增删顶点代价高（需重建矩阵）

### 无向图的对称性

无向图的邻接矩阵是**对称矩阵**：A[i][j] = A[j][i]

可以只存上三角或下三角，节省约一半空间。

### 示例

无向图 G，顶点 {v1, v2, v3, v4}，边 {(v1,v2),(v1,v3),(v2,v4),(v3,v4)}：

\`\`\`
     v1 v2 v3 v4
v1 [  0  1  1  0 ]
v2 [  1  0  0  1 ]
v3 [  1  0  0  1 ]
v4 [  0  1  1  0 ]
\`\`\``,
      },
      {
        id: "adjacency-list",
        title: "邻接表",
        type: "concept",
        content: `## 邻接表的定义

邻接表是图的链式存储结构。对每个顶点 vi，将其所有邻接点链成一个单链表，称为 vi 的**邻接表**。

整个图由顶点数组（每个元素含顶点信息和链表头指针）组成。

### C 语言结构体

\`\`\`c
#define MaxVertexNum 100

typedef char VertexType;

/* 边（弧）结点 */
typedef struct ArcNode {
    int adjvex;              /* 该弧所指向的顶点的位置 */
    struct ArcNode *next;    /* 指向下一条弧的指针 */
    /* int weight; */        /* 带权图可在此存权值 */
} ArcNode;

/* 顶点结点 */
typedef struct VNode {
    VertexType data;         /* 顶点信息 */
    ArcNode *first;          /* 指向第一条依附该顶点的弧 */
} VNode, AdjList[MaxVertexNum];

/* 图 */
typedef struct {
    AdjList vertices;        /* 邻接表 */
    int vexnum, arcnum;      /* 顶点数和弧数 */
} ALGraph;
\`\`\`

### 特点分析

**优点：**
- 空间复杂度 O(|V|+|E|)，适合稀疏图
- 遍历某顶点的所有邻居：O(degree(v))
- 增删边方便

**缺点：**
- 判断两顶点间是否有边：O(degree(v))，需遍历链表
- 有向图中求顶点的**入度**需遍历整个邻接表（或额外建逆邻接表）
- 链式结构，随机访问性能差

### 有向图的邻接表

有向图的邻接表只存出边（出边表）。若需要快速求入度，可同时建立**逆邻接表**（存入边）。

### 示例

同上图 G，邻接表表示：

\`\`\`
v1 -> [v2] -> [v3] -> NULL
v2 -> [v1] -> [v4] -> NULL
v3 -> [v1] -> [v4] -> NULL
v4 -> [v2] -> [v3] -> NULL
\`\`\``,
      },
      {
        id: "comparison",
        title: "两种存储方式对比",
        type: "comparison",
        content: `## 邻接矩阵 vs 邻接表

| 比较项目 | 邻接矩阵 | 邻接表 |
|----------|----------|--------|
| 空间复杂度 | O(V²) | O(V+E) |
| 判断边是否存在 | O(1) | O(degree) |
| 遍历某顶点的邻居 | O(V) | O(degree) |
| 求顶点的度 | O(V) | O(degree)（无向图）|
| 求有向图入度 | O(V) | O(V+E)（需遍历全表）|
| 适合场景 | 稠密图 | 稀疏图 |
| 表示唯一性 | 唯一 | 不唯一（链表顺序可变）|

### DFS/BFS 时间复杂度

| 遍历算法 | 邻接矩阵 | 邻接表 |
|----------|----------|--------|
| DFS | O(V²) | O(V+E) |
| BFS | O(V²) | O(V+E) |

原因：邻接矩阵中，找一个顶点的所有邻居需扫描整行（O(V)），共 V 个顶点，故 O(V²)。邻接表中，每条边只被访问常数次，故 O(V+E)。

### 选择原则

- **稠密图**（E 接近 V²）：选邻接矩阵。空间差距不大，但判断边更快。
- **稀疏图**（E 远小于 V²）：选邻接表。节省大量空间，遍历效率更高。
- **需要频繁判断边是否存在**：选邻接矩阵。
- **需要频繁遍历邻居**：选邻接表。

### 考研提示

考研题目中，若题目给出邻接矩阵，遍历复杂度按 O(V²) 计算；若给出邻接表，按 O(V+E) 计算。两种存储下，DFS/BFS 的遍历**结果**（序列）可能不同，因为邻接表中邻接点的顺序不固定。`,
      },
      {
        id: "orthogonal-multilist",
        title: "十字链表与邻接多重表",
        type: "detail",
        content: `## 十字链表（Orthogonal List）—— 有向图

邻接表求有向图的入度需要遍历整个表，效率低。十字链表将邻接表和逆邻接表结合，方便同时求出度和入度。

### 结点结构

**弧结点**：
\`\`\`
| tailvex | headvex | hlink | tlink | info |
\`\`\`
- tailvex：弧尾（起点）
- headvex：弧头（终点）
- hlink：弧头相同的下一条弧（同一终点的下一条入弧）
- tlink：弧尾相同的下一条弧（同一起点的下一条出弧）

**顶点结点**：
\`\`\`
| data | firstin | firstout |
\`\`\`
- firstin：以该顶点为弧头的第一条弧（第一条入弧）
- firstout：以该顶点为弧尾的第一条弧（第一条出弧）

### 示例

有向图：V0→V1, V0→V2, V2→V0, V2→V1

\`\`\`
顶点表：
V0: firstin→(2,0) firstout→(0,1)→(0,2)
V1: firstin→(0,1)→(2,1) firstout→NULL
V2: firstin→(0,2) firstout→(2,0)→(2,1)
\`\`\`

### 特点
- 求顶点出度：沿 firstout 的 tlink 链遍历
- 求顶点入度：沿 firstin 的 hlink 链遍历
- 空间：O(V+E)

---

## 邻接多重表（Adjacency Multilist）—— 无向图

邻接表中，无向图的每条边存储两次（两个顶点各一份），删除边需要找两个结点，不方便。邻接多重表让每条边只用一个结点表示。

### 边结点结构

\`\`\`
| mark | ivex | ilink | jvex | jlink | info |
\`\`\`
- mark：标志（是否被访问过）
- ivex, jvex：该边的两个端点
- ilink：依附于 ivex 的下一条边
- jlink：依附于 jvex 的下一条边

### 顶点结点

\`\`\`
| data | firstedge |
\`\`\`
- firstedge：依附于该顶点的第一条边

### 特点
- 每条边只有一个边结点（邻接表中有两个）
- 删除边只需删除一个结点并修改相关指针
- 标记边是否被访问过很方便（遍历时有用）
- 空间：O(V+E)

## 四种存储结构总结

| 存储结构 | 适用图类型 | 空间 | 特点 |
|----------|-----------|------|------|
| 邻接矩阵 | 有向/无向 | O(V²) | 判断边O(1)，适合稠密图 |
| 邻接表 | 有向/无向 | O(V+E) | 适合稀疏图，有向图求入度不便 |
| 十字链表 | 有向图 | O(V+E) | 方便求入度和出度 |
| 邻接多重表 | 无向图 | O(V+E) | 边只存一次，删边方便 |`,
      },
    ],
    exercises: [
      {
        id: "graph-basics-ex-01",
        title: "度的计算",
        description:
          "一个无向图有 8 个顶点和 10 条边。(1) 所有顶点的度之和是多少？(2) 若该图是完全图，应有多少条边？(3) 若用邻接矩阵存储，矩阵中有多少个 1？",
        difficulty: "easy",
        hints: [
          "无向图度之和公式：2|E|",
          "n个顶点的无向完全图边数：n(n-1)/2",
          "邻接矩阵中1的个数与边数的关系：无向图每条边对应矩阵中两个1",
        ],
        referenceSolution:
          "(1) 度之和 = 2×10 = 20。(2) 完全图边数 = 8×7/2 = 28。(3) 邻接矩阵中1的个数 = 2×10 = 20（无向图邻接矩阵对称，每条边对应两个1）。",
      },
      {
        id: "graph-basics-ex-02",
        title: "存储结构的选择",
        description:
          "有一个图 G，顶点数 |V|=1000，边数 |E|=2000。(1) 用邻接矩阵存储需要多少空间（以整型元素计）？(2) 用邻接表存储大约需要多少空间？(3) 应选哪种存储结构？说明理由。",
        difficulty: "medium",
        hints: [
          "邻接矩阵空间：V×V 个元素",
          "邻接表空间：V个顶点结点 + E个（或2E个）边结点",
          "稀疏图判断：E 远小于 V²",
        ],
        referenceSolution:
          "(1) 邻接矩阵：1000×1000 = 1,000,000 个整型元素。(2) 邻接表：1000个顶点结点 + 约4000个边结点（无向图每条边存两次）= 约5000个结点。(3) 应选邻接表。该图 E=2000 远小于 V²=1,000,000，是典型稀疏图，邻接表节省约200倍空间，且遍历效率更高。",
      },
    ],
  },
];

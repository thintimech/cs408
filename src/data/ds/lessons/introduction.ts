import { Lesson } from "@/types";

export const introductionLessons: Lesson[] = [
  {
    id: "ds-intro",
    title: "数据结构与算法基础概念",
    brief: "理解数据结构三要素、算法的定义与特性",
    analogy: "数据结构就像收纳方式——同样的衣服（数据），放衣柜（数组）方便找但难加新的，挂衣架（链表）方便加减但找起来慢。选什么结构取决于你最常做什么操作。",
    commonMistakes: [
      "数据结构不只是「存储结构」——它包含逻辑结构、存储结构和运算三个方面",
      "算法和程序不同——算法是有穷的（必须能终止），程序可以无穷运行（如操作系统）",
    ],
    keyTakeaways: [
      "数据结构三要素：逻辑结构（集合/线性/树/图）、存储结构（顺序/链式/索引/散列）、运算",
      "算法五个特性：有穷性、确定性、可行性、输入、输出",
      "好算法的标准：正确性、可读性、健壮性、高效率（时间）、低存储（空间）",
    ],
    sections: [
      {
        id: "ds-three-elements",
        title: "数据结构三要素",
        type: "concept",
        content: `## 逻辑结构

描述数据元素之间的逻辑关系，与存储无关：

| 逻辑结构 | 特点 | 例子 |
|----------|------|------|
| 集合 | 元素之间无关系 | 同一班级的学生 |
| 线性结构 | 一对一 | 数组、链表、栈、队列 |
| 树形结构 | 一对多 | 文件目录、组织架构 |
| 图形结构 | 多对多 | 社交网络、地图 |

## 存储结构（物理结构）

逻辑结构在计算机中的实现方式：

- **顺序存储**：用连续的存储单元存放（数组）。优点：随机访问；缺点：插入删除需移动
- **链式存储**：用指针表示元素间关系。优点：插入删除灵活；缺点：不能随机访问
- **索引存储**：建立索引表加速查找。优点：检索快；缺点：索引表占额外空间
- **散列存储**：根据关键字直接计算存储地址。优点：查找极快；缺点：可能冲突

## 运算

定义在逻辑结构上的操作（增删改查等），运算的实现依赖于存储结构。

> **考研重点**：同一种逻辑结构可以有不同的存储结构实现（如线性表可以用顺序存储或链式存储），不同的存储结构导致不同的算法实现和性能。`,
      },
      {
        id: "algorithm-definition",
        title: "算法的定义与特性",
        type: "concept",
        content: `## 算法的定义

算法是对特定问题求解步骤的一种描述，是指令的有限序列。

## 算法的五个特性

1. **有穷性**：算法必须在有限步骤后终止（程序不要求，如 OS 死循环监听）
2. **确定性**：每一步都有确切含义，无二义性
3. **可行性**：每一步都能通过基本运算有限次执行实现
4. **输入**：有零个或多个输入
5. **输出**：有一个或多个输出

## 好算法的评价标准

- **正确性**：能正确解决问题
- **可读性**：便于人理解
- **健壮性**：对非法输入能适当处理
- **高效性**：时间复杂度低
- **低存储**：空间复杂度低`,
      },
    ],
    exercises: [
      {
        id: "ds-concept-choice",
        title: "逻辑结构判断",
        description: "以下哪些是逻辑结构，哪些是存储结构？\n\n(1) 顺序表  (2) 线性表  (3) 链表  (4) 栈  (5) 队列  (6) 散列表",
        difficulty: "easy",
        hints: ["逻辑结构描述的是数据元素之间的关系", "存储结构是逻辑结构在计算机中的实现"],
        referenceSolution: "逻辑结构：(2)线性表、(4)栈、(5)队列\n存储结构：(1)顺序表、(3)链表\n\n(6)散列表既可以看作存储结构（散列存储），也可以看作一种数据结构（逻辑+存储+运算的整体）。\n\n注意：栈和队列是逻辑结构（定义了操作规则），它们可以用顺序存储或链式存储实现。",
      },
    ],
  },
  // PLACEHOLDER_NEXT
  {
    id: "complexity-analysis",
    title: "时间复杂度与空间复杂度",
    brief: "掌握大 O 记号、常见复杂度的计算方法和比较",
    analogy: "时间复杂度不是精确计时，而是看「数据量翻倍时，耗时大概翻几倍」——O(n) 翻1倍，O(n²) 翻4倍，O(logn) 几乎不变。",
    prerequisites: ["算法的基本概念", "数学：对数、求和公式"],
    commonMistakes: [
      "O(n) 不是说恰好执行 n 次——是说增长趋势与 n 成正比（忽略常数和低阶项）",
      "最好情况没有实际意义——考研中「时间复杂度」默认指最坏情况，除非特别说明",
      "O(1) 不是说只执行1次——是说执行次数与 n 无关（可能是100次，但不随 n 变化）",
      "两个嵌套循环不一定是 O(n²)——要看内层循环次数是否依赖外层变量",
    ],
    memoryAids: [
      "常见复杂度排序：O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(n³) < O(2ⁿ) < O(n!)",
      "看循环：单层 O(n)，嵌套 O(n²)，折半 O(logn)，递归看递推公式",
      "空间复杂度看「额外」空间——输入数据本身不算",
    ],
    keyTakeaways: [
      "时间复杂度 T(n) = O(f(n)) 表示算法执行时间的增长率与 f(n) 同阶",
      "只保留最高阶项，去掉系数：3n² + 5n + 1 → O(n²)",
      "常见计算方法：循环次数法、递推公式法（主定理）",
      "空间复杂度 S(n) 只计算辅助空间，不含输入数据",
      "递归算法空间复杂度 = 递归深度（栈帧数）",
    ],
    sections: [
      {
        id: "big-o-notation",
        title: "大 O 记号",
        type: "concept",
        content: `## 定义

若存在正常数 c 和 n₀，使得当 n ≥ n₀ 时，T(n) ≤ c·f(n)，则 T(n) = O(f(n))。

**直观理解**：忽略常数因子和低阶项后，T(n) 的增长不超过 f(n)。

## 计算规则

1. **加法规则**：T(n) = T₁(n) + T₂(n) = O(max(f₁(n), f₂(n)))
   - 顺序执行的代码段，取最大的那个
2. **乘法规则**：T(n) = T₁(n) × T₂(n) = O(f₁(n) × f₂(n))
   - 嵌套执行的代码段，相乘

## 常见复杂度对比

| 复杂度 | 名称 | n=10 | n=100 | n=1000 | 典型算法 |
|--------|------|------|-------|--------|----------|
| O(1) | 常数 | 1 | 1 | 1 | 数组按下标访问 |
| O(logn) | 对数 | 3 | 7 | 10 | 折半查找 |
| O(n) | 线性 | 10 | 100 | 1000 | 顺序查找 |
| O(nlogn) | 线性对数 | 33 | 664 | 9966 | 快排/归并 |
| O(n²) | 平方 | 100 | 10000 | 10⁶ | 冒泡/选择排序 |
| O(2ⁿ) | 指数 | 1024 | 10³⁰ | - | 暴力枚举子集 |

> n=100 时，O(n²) 要算 1 万次，O(2ⁿ) 要算 10³⁰ 次——这就是为什么算法效率如此重要。`,
      },
      {
        id: "complexity-calculation",
        title: "复杂度计算方法",
        type: "detail",
        content: `## 方法一：数循环

\`\`\`
// O(n)：循环执行 n 次
for (int i = 0; i < n; i++)
    sum += a[i];

// O(n²)：外层 n 次 × 内层 n 次
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        c[i][j] = a[i] + b[j];

// O(logn)：每次 i 翻倍，共执行 log₂n 次
for (int i = 1; i < n; i *= 2)
    count++;

// O(nlogn)：外层 n 次 × 内层 logn 次
for (int i = 0; i < n; i++)
    for (int j = 1; j < n; j *= 2)
        count++;
\`\`\`

## 方法二：看内层循环是否依赖外层

\`\`\`
// 这不是 O(n²)！
for (int i = 0; i < n; i++)
    for (int j = 0; j < i; j++)  // j 从 0 到 i-1
        count++;
// 总次数 = 0+1+2+...+(n-1) = n(n-1)/2 → O(n²)
// 虽然结果还是 O(n²)，但要会分析过程
\`\`\`

\`\`\`
// 这个呢？
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= i; j *= 2)
        count++;
// 内层执行 log₂i 次，总次数 = Σlog₂i (i=1到n) ≈ nlog₂n → O(nlogn)
\`\`\`

## 方法三：递归的时间复杂度

递归算法用递推公式分析：

\`\`\`
// 斐波那契递归
int fib(int n) {
    if (n <= 1) return n;
    return fib(n-1) + fib(n-2);
}
// T(n) = T(n-1) + T(n-2) + O(1) → O(2ⁿ)
\`\`\`

\`\`\`
// 二分查找递归
int bsearch(int a[], int low, int high, int key) {
    if (low > high) return -1;
    int mid = (low + high) / 2;
    if (a[mid] == key) return mid;
    if (a[mid] > key) return bsearch(a, low, mid-1, key);
    return bsearch(a, mid+1, high, key);
}
// T(n) = T(n/2) + O(1) → O(logn)
\`\`\`

**主定理（Master Theorem）简化版**：
- T(n) = aT(n/b) + O(nᵈ)
  - 若 d > log_b(a)：T(n) = O(nᵈ)
  - 若 d = log_b(a)：T(n) = O(nᵈ logn)
  - 若 d < log_b(a)：T(n) = O(n^(log_b(a)))

## 空间复杂度

只计算**额外辅助空间**：
- 原地排序（如冒泡）：O(1)
- 归并排序需要临时数组：O(n)
- 递归调用栈：深度 × 每层空间
  - 快排平均递归深度 O(logn)，每层 O(1) → 空间 O(logn)
  - 快排最坏递归深度 O(n) → 空间 O(n)`,
      },
    ],
    exercises: [
      {
        id: "complexity-calc-1",
        title: "分析循环复杂度",
        description: "分析以下代码的时间复杂度：\n\nint count = 0;\nfor (int i = 1; i <= n; i *= 2)\n    for (int j = 0; j < i; j++)\n        count++;",
        difficulty: "medium",
        hints: ["外层循环 i 取值为 1, 2, 4, ..., 2^k（其中 2^k ≤ n）", "内层循环执行 i 次", "总次数 = 1 + 2 + 4 + ... + 2^k"],
        referenceSolution: "外层循环执行 ⌊log₂n⌋ + 1 次，i 依次取 1, 2, 4, ..., 2^⌊log₂n⌋。\n内层循环每次执行 i 次。\n总次数 = 1 + 2 + 4 + ... + 2^⌊log₂n⌋ = 2^(⌊log₂n⌋+1) - 1 ≈ 2n\n\n时间复杂度：O(n)\n\n注意：虽然有两层循环，但不是 O(n²) 也不是 O(nlogn)！关键是内层循环次数随外层变化。",
      },
      {
        id: "complexity-compare",
        title: "复杂度排序",
        description: "将以下时间复杂度从小到大排列：\nO(n²)、O(2ⁿ)、O(nlogn)、O(n!)、O(logn)、O(n)、O(1)、O(n³)、O(√n)",
        difficulty: "easy",
        hints: ["对数增长最慢，指数和阶乘增长最快", "√n = n^(1/2)，介于 logn 和 n 之间"],
        referenceSolution: "O(1) < O(logn) < O(√n) < O(n) < O(nlogn) < O(n²) < O(n³) < O(2ⁿ) < O(n!)\n\n验证（n=16）：\n1 < 4 < 4 < 16 < 64 < 256 < 4096 < 65536 < 2×10¹³",
      },
    ],
  },
];

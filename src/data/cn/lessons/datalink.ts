import { Lesson } from "@/types";

export const datalinkLessons: Lesson[] = [
  {
    id: "framing-error",
    title: "成帧与差错控制",
    brief: "帧定界方法与CRC循环冗余校验",
    sections: [
      {
        id: "framing",
        title: "成帧方法",
        type: "concept",
        content: `## 成帧 (Framing)

数据链路层将网络层的分组封装成帧，需要确定帧的边界。

### 1. 字符计数法

在帧头用一个字段标明帧的字符数。
- 缺点：如果计数值出错，后续所有帧都会出错（失去同步）

### 2. 字符填充的首尾定界符法

- 用特殊字符 SOH (Start of Header) 和 EOT (End of Transmission) 标记帧的开始和结束
- 如果数据中出现这些特殊字符，需要在前面插入转义字符 ESC
- 如果数据中出现 ESC，则再插入一个 ESC

### 3. 比特填充的首尾标志法

- 用特殊比特模式 01111110 (0x7E) 作为帧的开始和结束标志
- 发送方：数据中每遇到连续5个1，就插入一个0
- 接收方：每遇到连续5个1后面的0，就删除该0
- 应用：HDLC、PPP协议

### 4. 违规编码法

- 利用物理层编码的冗余来标记帧边界
- 例如曼彻斯特编码中，"高-高"和"低-低"不是有效数据编码，可用作定界符`,
      },
      {
        id: "crc",
        title: "CRC循环冗余校验",
        type: "walkthrough",
        content: `## CRC校验原理

CRC (Cyclic Redundancy Check) 是数据链路层最常用的差错检测方法。

### 基本思想

1. 发送方和接收方约定一个**生成多项式 G(x)**
2. 发送方根据数据和G(x)计算出**冗余码(FCS)**附加在数据后面
3. 接收方用G(x)对收到的帧做除法，余数为0则无错

### 计算步骤

设数据为 M，生成多项式为 G(x)，G(x) 的阶为 r：

1. 在 M 后面添加 r 个 0，得到 M x 2^r
2. 用 M x 2^r 对 G(x) 对应的二进制数做**模2除法**
3. 得到 r 位余数 R（即 FCS）
4. 发送 M 后面拼接 R

### 模2运算规则

- 模2加法/减法 = 异或 (XOR)
- 不借位、不进位`,
        steps: [
          {
            description: "例题：数据 M = 101001，生成多项式 G(x) = x^3 + x + 1，对应二进制 1011，阶 r = 3",
          },
          {
            description: "第1步：M 后面添加 r=3 个 0，得到 101001000",
          },
          {
            description: "第2步：用 101001000 除以 1011（模2除法）",
            pseudocode: "101001000 / 1011\n\n101001000\n1011      (XOR)\n--------\n 0011\n  0110\n  0000  (首位为0，商0)\n  ----\n  01100\n  0000  (首位为0，商0)\n  -----\n  011000\n  0000  (首位为0，商0)\n  ------\n  1100 0\n  1011  (XOR)\n  ------\n   1110\n   1011 (XOR)\n   ----\n    101  <- 余数 R",
          },
          {
            description: "第3步：FCS = 101，发送的帧为 101001 101",
          },
          {
            description: "验证：接收方用 101001101 除以 1011，若余数为 000 则无错",
          },
        ],
      },
    ],
    exercises: [
      {
        id: "ex-crc",
        title: "CRC计算",
        description: "数据为 1101011，生成多项式 G(x) = x^4 + x + 1。求CRC校验码（FCS），并写出实际发送的比特序列。",
        difficulty: "medium",
        hints: [
          "G(x) = x^4 + x + 1 对应二进制 10011，阶 r = 4",
          "在数据后面添加4个0",
          "做模2除法，求余数",
        ],
        referenceSolution: "G(x) = x^4 + x + 1 -> 10011, r = 4\n数据: 1101011, 添加4个0: 11010110000\n模2除法: 11010110000 / 10011\n\n11010 XOR 10011 = 01001\n10011 XOR 10011 = 00000\n后续位逐位下移: 00001 -> 00010 -> 00100 -> 01000 -> 10000\n10000 XOR 10011 = 00011\n\nFCS = 0011\n发送序列: 1101011 0011\n\n验证: 11010110011 / 10011 余数为 0000，无错误。",
      },
    ],
    keyTakeaways: [
      "比特填充法用01111110作为帧定界，数据中连续5个1后插入0",
      "CRC只能检错不能纠错",
      "CRC计算的核心是模2除法（异或运算）",
      "FCS的位数等于生成多项式的阶",
    ],
  },
  {
    id: "flow-control",
    title: "流量控制与可靠传输",
    brief: "停止等待、GBN回退N帧、SR选择重传协议",
    sections: [
      {
        id: "stop-wait",
        title: "停止等待协议",
        type: "concept",
        content: `## 停止等待协议 (Stop-and-Wait)

### 工作原理

1. 发送方发送一帧后，**停止发送**，等待接收方确认
2. 收到 ACK 后发送下一帧
3. 收到 NAK 或超时则重传

### 关键机制

- **超时重传**：发送方启动计时器，超时未收到ACK则重传
- **帧编号**：用0和1交替编号（1bit即可），解决重复帧问题
- **ACK编号**：ACK携带期望收到的下一帧编号

### 信道利用率

U = T_f / (T_f + RTT + T_a)

- T_f：发送一帧的时间
- RTT：往返传播时延
- T_a：发送ACK的时间（通常忽略）

停止等待协议的信道利用率很低，尤其在长延迟链路上。`,
      },
      {
        id: "gbn",
        title: "GBN回退N帧协议",
        type: "walkthrough",
        content: `## GBN (Go-Back-N) 协议

### 核心思想

- 发送方可以连续发送多个帧（窗口内），不必等待逐个确认
- 接收方采用**累积确认**：ACK(n) 表示 n 及之前的帧都已正确接收
- 接收方只按序接收，失序帧直接丢弃
- 发送方超时后，从出错帧开始**重传窗口内所有帧**

### 参数

- 发送窗口大小：W_T (1 < W_T <= 2^n - 1，n为序号位数)
- 接收窗口大小：W_R = 1（只能按序接收）

### 为什么发送窗口 <= 2^n - 1？

如果 W_T = 2^n，当所有ACK丢失时，发送方重传的帧序号与新帧序号相同，接收方无法区分新帧和重传帧。`,
        steps: [
          {
            description: "假设发送窗口 W_T = 4，序号位数 n = 3（序号0~7）。发送方连续发送帧0、1、2、3。",
            state: { window: [0, 1, 2, 3], sent: [0, 1, 2, 3], acked: [] },
          },
          {
            description: "帧0、1正确到达，接收方发送ACK1。帧2在传输中丢失。",
            state: { window: [0, 1, 2, 3], received: [0, 1], lost: [2] },
          },
          {
            description: "帧3到达接收方，但因为帧2未收到（失序），接收方丢弃帧3，重发ACK1。",
            state: { window: [0, 1, 2, 3], discarded: [3], lastAck: 1 },
          },
          {
            description: "发送方收到ACK1，窗口滑动：可发送帧4、5。发送方继续发送帧4、5。",
            state: { window: [2, 3, 4, 5], sent: [4, 5] },
          },
          {
            description: "帧2的超时计时器到期，发送方回退，重传帧2、3、4、5（窗口内所有未确认帧）。",
            state: { retransmit: [2, 3, 4, 5] },
          },
          {
            description: "接收方按序收到帧2、3、4、5，发送ACK5。发送方窗口滑动到帧6。",
            state: { window: [6, 7, 0, 1], acked: [5] },
          },
        ],
      },
      {
        id: "sr",
        title: "SR选择重传协议",
        type: "detail",
        content: `## SR (Selective Repeat) 协议

### 与GBN的区别

| 特性 | GBN | SR |
|------|-----|-----|
| 接收窗口 | 1 | > 1 |
| 确认方式 | 累积确认 | 逐一确认 |
| 失序帧处理 | 丢弃 | 缓存 |
| 重传策略 | 回退重传所有 | 只重传出错帧 |

### SR协议要点

- 发送窗口 W_T = 接收窗口 W_R
- 窗口大小限制：W_T + W_R <= 2^n，即 W_T <= 2^(n-1)
- 接收方缓存失序帧，等待缺失帧到达后一起交付上层
- 每个帧都有独立的确认和计时器

### 为什么 W_T <= 2^(n-1)？

如果窗口过大，当ACK全部丢失时，发送方重传的旧帧序号可能落在接收方新窗口范围内，导致接收方将旧帧误认为新帧。

### 适用场景

- 链路误码率较高时，SR比GBN效率更高
- GBN实现简单，适合误码率低的场景`,
      },
    ],
    exercises: [
      {
        id: "ex-gbn-window",
        title: "GBN窗口大小",
        description: "若数据帧的序号字段为3位，采用GBN协议，求：(1) 发送窗口的最大值 (2) 若发送窗口为5，发送方已发送0~4号帧，收到ACK2，此时发送窗口的范围是什么？",
        difficulty: "medium",
        hints: [
          "序号字段3位，序号范围0~7",
          "GBN发送窗口最大值 = 2^n - 1",
          "ACK2表示2号及之前的帧都已确认",
        ],
        referenceSolution: "(1) 发送窗口最大值 = 2^3 - 1 = 7\n(2) 收到ACK2，表示帧0、1、2已确认。窗口滑动，新窗口范围为 [3, 4, 5, 6, 7]（大小仍为5）。其中帧3、4已发送未确认，帧5、6、7可以发送。",
      },
    ],
    keyTakeaways: [
      "停止等待协议简单但信道利用率低",
      "GBN采用累积确认，失序帧丢弃，超时回退重传",
      "SR逐一确认，缓存失序帧，只重传出错帧",
      "GBN窗口 <= 2^n - 1，SR窗口 <= 2^(n-1)",
    ],
    relatedLessons: ["framing-error"],
  },
  {
    id: "mac",
    title: "介质访问控制",
    brief: "ALOHA、CSMA/CD、CSMA/CA与令牌环协议",
    sections: [
      {
        id: "aloha",
        title: "ALOHA协议",
        type: "concept",
        content: `## 随机访问协议

当多个站点共享同一信道时，需要解决"谁先发"的问题。

### 纯ALOHA

- 任何站点有数据就发送
- 发生冲突后等待随机时间重发
- 最大吞吐量：S = G x e^(-2G)，最大值约 **18.4%**（G=0.5时）

### 时隙ALOHA

- 将时间分成等长时隙，站点只能在时隙开始时发送
- 减少了冲突的"易损期"（从2T减为T）
- 最大吞吐量：S = G x e^(-G)，最大值约 **36.8%**（G=1时）

### 对比

| 协议 | 易损期 | 最大利用率 |
|------|--------|-----------|
| 纯ALOHA | 2T | 18.4% |
| 时隙ALOHA | T | 36.8% |`,
      },
      {
        id: "csma-cd",
        title: "CSMA/CD协议",
        type: "detail",
        content: `## CSMA/CD (载波监听多路访问/冲突检测)

应用于**有线局域网**（以太网）。

### 工作流程

1. **先听后发**：发送前监听信道是否空闲
2. **边发边听**：发送过程中持续检测冲突
3. **冲突停发**：检测到冲突立即停止发送
4. **随机延迟**：等待随机时间后重试

### 关键参数

**争用期（冲突窗口）= 2t**

- t 为单程传播时延
- 在发送后 2t 时间内未检测到冲突，则本次发送成功
- 以太网争用期 = 51.2 us（10Mbps以太网）

### 最小帧长

最小帧长 = 2t x 数据率

- 10Mbps以太网：最小帧长 = 51.2us x 10Mbps = 512bit = 64B
- 如果帧长 < 最小帧长，发送方在发完帧后才检测到冲突，无法判断是否成功

### 截断二进制指数退避算法

冲突后等待时间 = r x 2t（争用期）

- 第 k 次冲突后，r 从 {0, 1, ..., 2^k - 1} 中随机选取
- k = min(重传次数, 10)
- 重传16次仍失败则放弃

### CSMA/CD 不适用于无线网络

因为无线网络存在**隐蔽站问题**：A和C都在B的范围内但互相听不到，可能同时向B发送导致冲突。`,
      },
      {
        id: "csma-ca",
        title: "CSMA/CA与令牌环",
        type: "detail",
        content: `## CSMA/CA (载波监听多路访问/冲突避免)

应用于**无线局域网**（WiFi, IEEE 802.11）。

### 为什么不用CD而用CA？

- 无线信号衰减大，发送时自身信号远强于其他站信号，难以检测冲突
- 存在隐蔽站问题

### 工作机制

1. 发送前监听信道
2. 信道空闲：等待 DIFS (分布式帧间间隔) 后发送
3. 信道忙：随机退避
4. 接收方收到后等待 SIFS (短帧间间隔) 后发送 ACK

### RTS/CTS 机制（可选）

解决隐蔽站问题：
1. 发送方先发 RTS (Request To Send)
2. 接收方回复 CTS (Clear To Send)
3. 其他站听到CTS后保持静默
4. 发送方收到CTS后开始发送数据

### NAV (网络分配向量)

RTS/CTS中包含预计占用信道的时间，其他站设置NAV计时器，在此期间不发送。

---

## 令牌环 (Token Ring)

### 工作原理

- 令牌在环上循环传递
- 站点收到令牌后才能发送数据
- 发送完毕后释放令牌

### 特点

- 无冲突（确定性访问）
- 负载重时效率高
- 负载轻时有令牌等待延迟
- 环维护复杂（令牌丢失、站点故障）`,
      },
    ],
    exercises: [
      {
        id: "ex-csma-cd",
        title: "CSMA/CD最小帧长",
        description: "某以太网数据率为 100Mbps，两站之间最远距离为 2km，信号传播速率为 2x10^8 m/s。求：(1) 争用期 (2) 最小帧长",
        difficulty: "medium",
        hints: [
          "争用期 = 2 x 单程传播时延",
          "单程传播时延 = 距离 / 传播速率",
          "最小帧长 = 争用期 x 数据率",
        ],
        referenceSolution: "(1) 单程传播时延 t = 2000m / (2x10^8 m/s) = 10us\n争用期 = 2t = 20us\n(2) 最小帧长 = 20us x 100Mbps = 20x10^-6 x 100x10^6 = 2000bit = 250B",
      },
    ],
    keyTakeaways: [
      "CSMA/CD用于有线网（以太网），边发边听",
      "CSMA/CA用于无线网（WiFi），冲突避免",
      "最小帧长 = 争用期 x 数据率",
      "令牌环是确定性访问协议，无冲突",
    ],
    relatedLessons: ["framing-error", "flow-control"],
  },
  {
    id: "ppp-protocol",
    title: "PPP协议",
    brief: "点对点协议的帧格式、工作过程与透明传输",
    analogy: "PPP像两个人之间的专线电话——不需要考虑谁先说话（点对点，无竞争），但需要约定通话规则：先拨号建立连接（LCP协商），确认身份（认证），约定用什么语言（NCP配置网络层），然后才能正式通话，挂电话时也要有礼貌地说再见（LCP终止）。",
    prerequisites: ["成帧与差错控制", "数据链路层基本概念"],
    commonMistakes: [
      "PPP是点对点协议——不能用于多点连接（多点用以太网等）",
      "PPP只检测差错不纠正差错——发现错误直接丢弃，由上层负责重传",
      "PPP不提供流量控制——它假设链路质量足够好",
      "PPP的透明传输用字节填充（异步）或零比特填充（同步）——不是字符计数法",
    ],
    memoryAids: [
      "PPP帧格式：7E + FF + 03 + 协议 + 数据 + FCS + 7E",
      "PPP工作阶段：链路建立(LCP)→认证(PAP/CHAP)→网络层配置(NCP)→数据传输→终止",
      "PPP三个组成：数据链路层协议(帧格式) + LCP(链路控制) + NCP(网络层配置)",
      "PPP不提供：多点连接、流量控制、纠错、序号",
    ],
    keyTakeaways: [
      "PPP是Internet中使用最广泛的点对点数据链路层协议",
      "PPP帧用标志字节7E定界，透明传输用字节填充(异步)或零比特填充(同步)",
      "PPP只检错不纠错，不提供流量控制和可靠传输",
      "PPP工作过程：LCP建立链路→认证→NCP配置→数据传输→LCP终止",
      "PPP支持PAP(明文)和CHAP(挑战握手)两种认证方式",
    ],
    relatedLessons: ["framing-error", "ethernet"],
    sections: [
      {
        id: "ppp-frame",
        title: "PPP帧格式",
        type: "concept",
        content: `## PPP帧格式

| 标志 | 地址 | 控制 | 协议 | 数据 | FCS | 标志 |
|------|------|------|------|------|-----|------|
| 7E | FF | 03 | 2字节 | ≤1500字节 | 2字节 | 7E |

- **标志(Flag)**：0x7E，标识帧的开始和结束
- **地址(Address)**：0xFF，广播地址（点对点无实际意义）
- **控制(Control)**：0x03，无编号帧
- **协议(Protocol)**：标识数据部分的协议类型
  - 0x0021：IP数据报
  - 0xC021：LCP数据
  - 0x8021：NCP(IPCP)数据
- **FCS**：CRC校验（只检错不纠错）

## 透明传输

### 异步传输（字节填充）

数据中出现0x7E时，用转义序列替换：
- 0x7E → 0x7D 0x5E
- 0x7D → 0x7D 0x5D
- 控制字符(ASCII<0x20) → 0x7D + (字符 XOR 0x20)

### 同步传输（零比特填充）

与HDLC相同：数据中每遇到5个连续1就插入一个0。`,
      },
      {
        id: "ppp-phases",
        title: "PPP工作过程",
        type: "walkthrough",
        content: `PPP链路从建立到终止的完整过程`,
        steps: [
          {
            description: "链路静止状态(Dead)：物理层不可用，无连接",
            state: { phase: "dead" },
          },
          {
            description: "链路建立(Establish)：检测到载波信号，发送LCP配置请求，协商MRU、认证方式等参数",
            state: { phase: "establish", protocol: "LCP" },
          },
          {
            description: "认证(Authenticate)：若协商了认证，执行PAP或CHAP认证。认证失败则回到Dead状态",
            state: { phase: "authenticate", protocol: "PAP/CHAP" },
          },
          {
            description: "网络层配置(Network)：通过NCP(如IPCP)协商网络层参数，分配IP地址等",
            state: { phase: "network", protocol: "NCP/IPCP" },
          },
          {
            description: "数据传输(Open)：链路正常工作，传输网络层数据报",
            state: { phase: "open", note: "正常数据传输阶段" },
          },
          {
            description: "链路终止(Terminate)：一方发送LCP终止请求，释放链路资源，回到Dead状态",
            state: { phase: "terminate", protocol: "LCP" },
          },
        ],
      },
    ],
    exercises: [
      {
        id: "ppp-ex1",
        title: "PPP特性判断",
        description: "判断以下说法的正误：\n(1) PPP协议可以用于以太网中多台主机之间的通信\n(2) PPP协议提供差错检测但不提供差错纠正\n(3) PPP帧的地址字段为0xFF表示这是一个广播帧\n(4) PPP协议的数据部分最大长度默认为1500字节\n(5) PPP使用字节填充实现透明传输时，数据中的0x7E被替换为0x7D 0x5E",
        difficulty: "easy" as const,
        hints: ["PPP是点对点协议", "PPP的地址字段是固定值"],
        referenceSolution: "(1) 错误。PPP是点对点协议，只能用于两个节点之间的直接连接，不能用于多点共享链路。\n(2) 正确。PPP使用FCS(CRC)检测差错，发现错误直接丢弃帧，不进行纠错和重传。\n(3) 错误。0xFF是固定值，因为PPP是点对点的，不存在多个接收方，地址字段无实际意义。\n(4) 正确。PPP默认MRU(最大接收单元)为1500字节，可通过LCP协商修改。\n(5) 正确。这是PPP异步传输时的字节填充规则。",
      },
    ],
  },
];

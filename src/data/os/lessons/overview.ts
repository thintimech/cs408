import { Lesson } from "@/types";

export const overviewLessons: Lesson[] = [
  {
    id: "os-concepts",
    title: "操作系统基本概念",
    brief: "操作系统的定义、功能、特征与发展历程",
    analogy: "操作系统像酒店的总管家——硬件是酒店建筑（房间、厨房、大厅），应用程序是住客，OS负责分配房间（内存管理）、安排用餐顺序（进程调度）、管理钥匙（文件系统）、协调服务员（IO管理），让所有住客都能舒适地使用酒店设施而互不干扰。",
    prerequisites: ["计算机系统基本组成"],
    commonMistakes: [
      "并发和并行不同——并发是宏观同时微观交替（单核），并行是真正同时执行（多核）",
      "OS本身不是一直在运行——OS是被动的，只在中断/异常/系统调用时才介入",
      "分时系统不是实时系统——分时追求响应时间短（交互性），实时追求截止时间前完成（可靠性）",
      "虚拟性依赖于并发性——没有并发就不需要虚拟化资源",
    ],
    memoryAids: [
      "OS四大特征：并发、共享、虚拟、异步（并共虚异）",
      "OS五大功能：进程管理、内存管理、文件管理、IO管理、用户接口",
      "发展历程：手工→批处理(单道→多道)→分时→实时→网络/分布式",
      "并发是最基本特征——其他三个都以并发为前提",
    ],
    keyTakeaways: [
      "OS是管理硬件和软件资源、为用户提供服务的系统软件",
      "四大特征中并发是最基本的，共享是最基本的目标",
      "多道批处理系统引入了并发，是OS形成的标志",
      "分时系统的关键参数是时间片大小",
      "实时系统分为硬实时（绝对不能超时）和软实时（偶尔超时可接受）",
    ],
    sections: [
      {
        id: "os-definition",
        title: "操作系统的定义与功能",
        type: "concept",
        content: `## 操作系统的定义

操作系统(Operating System)是控制和管理计算机硬件与软件资源、合理组织计算机工作流程、为用户提供方便使用计算机的接口的系统软件。

## 操作系统的角色

1. **资源管理者**：管理处理器、存储器、IO设备、文件等资源
2. **用户与硬件的接口**：命令接口（联机/脱机）、程序接口（系统调用）、GUI
3. **扩充机器**：将裸机扩展为功能更强的虚拟机器

## 操作系统的五大功能

| 功能 | 管理对象 | 核心任务 |
|------|----------|----------|
| 进程管理 | CPU | 进程调度、同步互斥、通信 |
| 内存管理 | 主存 | 分配回收、地址转换、保护共享 |
| 文件管理 | 外存 | 文件组织、目录管理、存取控制 |
| IO管理 | IO设备 | 缓冲、设备分配、驱动 |
| 用户接口 | 用户 | 命令接口、系统调用、GUI |`,
      },
      {
        id: "os-characteristics",
        title: "操作系统的四大特征",
        type: "concept",
        content: `## 四大特征

### 1. 并发 (Concurrency) —— 最基本特征

- 多个事件在同一时间段内发生（宏观同时，微观交替）
- 并发 vs 并行：并发是逻辑上同时（单CPU交替），并行是物理上同时（多CPU）
- 引入并发的目的：提高资源利用率和系统吞吐量

### 2. 共享 (Sharing)

- 系统中的资源可供多个并发进程共同使用
- **互斥共享**：一段时间内只允许一个进程访问（如打印机）
- **同时共享**：允许多个进程"同时"访问（如磁盘文件，宏观同时微观交替）

### 3. 虚拟 (Virtual)

- 通过某种技术将物理实体变为若干逻辑上的对应物
- **时分复用**：虚拟处理器（多道程序让每个进程感觉独占CPU）
- **空分复用**：虚拟存储器（让每个进程感觉拥有很大的内存空间）

### 4. 异步 (Asynchronism)

- 进程以不可预知的速度向前推进
- 多道程序环境下，进程的执行顺序和执行时间不确定
- OS必须保证：无论进程以何种顺序推进，结果都相同

> **依赖关系**：并发 → 共享（并发才需要共享）→ 虚拟（共享需要虚拟化）→ 异步（并发导致异步）`,
      },
      {
        id: "os-history",
        title: "操作系统的发展历程",
        type: "detail",
        content: `## 发展阶段

| 阶段 | 特点 | 优缺点 |
|------|------|--------|
| 手工操作 | 用户独占全机，人工装卸纸带 | CPU利用率极低 |
| 单道批处理 | 自动连续处理作业，内存中只有一道程序 | 资源利用率仍低（IO时CPU空闲） |
| **多道批处理** | 内存中同时存放多道程序，宏观并行 | 资源利用率高，但无交互能力 |
| 分时系统 | 时间片轮转，多用户交互 | 响应快，但不能处理紧急任务 |
| 实时系统 | 在截止时间前完成任务 | 可靠性高，但资源利用率可能低 |

## 多道批处理系统（重点）

**多道程序设计**的核心思想：当一个程序等待IO时，CPU转去执行另一个程序。

优点：
- CPU利用率高
- 系统吞吐量大
- 资源利用率高

缺点：
- 用户无法交互
- 作业周转时间长

## 分时系统 vs 实时系统

| | 分时系统 | 实时系统 |
|--|---------|---------|
| 目标 | 快速响应用户交互 | 在截止时间前完成 |
| 交互性 | 强 | 弱（通常无人值守） |
| 可靠性要求 | 一般 | 极高 |
| 典型应用 | Unix/Linux终端 | 导弹控制、工业控制 |`,
      },
    ],
    exercises: [
      {
        id: "os-concept-ex1",
        title: "特征辨析",
        description: "判断以下说法的正误：\n(1) 单处理器系统中不可能实现并行\n(2) 没有并发就没有共享的必要\n(3) 实时系统不需要多道程序设计\n(4) 批处理系统的主要优点是交互性好",
        difficulty: "easy" as const,
        hints: ["注意并发和并行的区别", "思考各特征之间的依赖关系"],
        referenceSolution: "(1) 错误。单处理器可以实现并发（交替执行），但不能实现并行。不过如果考虑CPU和IO设备的并行，单处理器也有并行。\n(2) 正确。如果只有一个程序运行，资源独占，不需要共享机制。\n(3) 错误。实时系统也可以使用多道程序设计来提高资源利用率，只是要保证实时任务优先。\n(4) 错误。批处理系统的优点是资源利用率高和吞吐量大，缺点恰恰是没有交互性。",
      },
    ],
  },
  {
    id: "interrupt-syscall",
    title: "中断、异常与系统调用",
    brief: "理解中断/异常的分类与处理过程，掌握系统调用的机制",
    analogy: "中断像上课时有人敲门——老师（CPU）暂停讲课，处理完敲门的事再继续。外中断是外面有人敲门（IO完成），内中断（异常）是学生举手提问（程序出错或请求服务）。系统调用是学生按规矩举手——必须通过这个正式渠道才能请求老师帮忙。",
    prerequisites: ["操作系统基本概念"],
    commonMistakes: [
      "中断和异常不完全相同——中断来自CPU外部（异步），异常来自CPU内部（同步）",
      "系统调用不是普通函数调用——系统调用会从用户态切换到核心态（通过陷入指令/trap）",
      "陷入指令(trap/int)是用户态执行的——它是唯一一条在用户态执行但能引起态切换的指令",
      "中断处理和异常处理的返回点不同——中断返回下一条指令，故障返回当前指令重新执行",
    ],
    memoryAids: [
      "中断分类：外中断(IO中断/时钟中断) + 内中断/异常(故障fault/自陷trap/终止abort)",
      "态切换方向：用户态→核心态只能通过中断/异常，核心态→用户态通过设置PSW",
      "系统调用过程：传参→陷入指令→查系统调用表→执行→返回",
      "特权指令只能在核心态执行，非特权指令两种态都能执行",
    ],
    keyTakeaways: [
      "中断是OS夺回CPU控制权的唯一途径",
      "外中断（中断）：来自CPU外部，异步，如IO完成、时钟",
      "内中断（异常）：来自CPU内部，同步，如除零、缺页、陷入",
      "系统调用通过陷入指令(trap/int 80h)实现用户态→核心态切换",
      "用户程序不能直接执行特权指令，必须通过系统调用请求OS服务",
      "中断处理过程：关中断→保存断点和PSW→识别中断源→转中断服务程序",
    ],
    relatedLessons: ["os-concepts"],
    sections: [
      {
        id: "interrupt-classification",
        title: "中断与异常的分类",
        type: "concept",
        content: `## 中断的分类

### 外中断（中断，Interrupt）

来自CPU**外部**的事件，与当前执行的指令无关（异步）：
- **IO中断**：设备完成IO操作后通知CPU
- **时钟中断**：定时器到时，用于时间片轮转
- **硬件故障中断**：如电源掉电、内存校验错

### 内中断（异常，Exception）

来自CPU**内部**的事件，与当前执行的指令有关（同步）：

| 类型 | 说明 | 处理后 |
|------|------|--------|
| 故障(Fault) | 可恢复的错误（如缺页） | 返回当前指令重新执行 |
| 自陷(Trap) | 有意为之（如系统调用、断点） | 返回下一条指令 |
| 终止(Abort) | 不可恢复的错误（如硬件故障） | 终止进程 |

## 中断与异常的对比

| | 中断(外中断) | 异常(内中断) |
|--|-------------|-------------|
| 来源 | CPU外部 | CPU内部 |
| 时机 | 异步（随时可能） | 同步（执行指令时） |
| 例子 | IO完成、时钟 | 除零、缺页、trap |
| 可否屏蔽 | 可屏蔽/不可屏蔽 | 不可屏蔽 |`,
      },
      {
        id: "cpu-mode",
        title: "处理器状态与特权级",
        type: "concept",
        content: `## 用户态与核心态

| | 用户态(目态) | 核心态(管态) |
|--|-------------|-------------|
| PSW标志 | 0 | 1 |
| 可执行指令 | 非特权指令 | 所有指令 |
| 可访问空间 | 用户空间 | 全部空间 |
| 运行的程序 | 用户程序 | OS内核 |

## 特权指令

只能在核心态执行的指令：
- IO指令
- 置中断指令（开/关中断）
- 存取特殊寄存器指令（如修改PSW）
- 清内存指令
- 停机指令

## 态切换

- **用户态 → 核心态**：只能通过中断或异常（硬件自动完成）
- **核心态 → 用户态**：执行一条特权指令修改PSW（OS主动让出）

> 用户程序想要OS服务，必须通过**系统调用**（本质是执行陷入指令触发异常）。`,
      },
      {
        id: "syscall-mechanism",
        title: "系统调用机制",
        type: "detail",
        content: `## 系统调用的定义

系统调用是OS提供给应用程序的接口，是用户程序请求OS服务的唯一合法途径。

## 系统调用的分类

| 类别 | 功能 | 例子 |
|------|------|------|
| 进程控制 | 创建/终止进程 | fork, exit, wait |
| 文件管理 | 打开/读写/关闭文件 | open, read, write, close |
| 设备管理 | 请求/释放设备 | ioctl, read, write |
| 信息维护 | 获取系统信息 | getpid, time |
| 通信 | 进程间通信 | pipe, shmget, send |

## 系统调用的执行过程

1. 用户程序将参数放入寄存器（或压栈）
2. 执行**陷入指令**（如 INT 80h / syscall）
3. 硬件自动：保存断点和PSW，切换到核心态
4. 根据系统调用号查**系统调用表**，找到对应处理函数
5. 执行系统调用处理函数
6. 恢复现场，返回用户态，继续执行用户程序

## 系统调用 vs 库函数 vs API

| | 系统调用 | 库函数 | API |
|--|---------|--------|-----|
| 运行态 | 核心态 | 用户态(可能调用系统调用) | 用户态 |
| 提供者 | OS内核 | 语言运行库 | 编程接口规范 |
| 例子 | write() | printf() | POSIX |

> printf() 是库函数，它内部会调用 write() 系统调用来实际输出。`,
      },
      {
        id: "interrupt-handling",
        title: "中断处理过程",
        type: "walkthrough",
        content: `中断处理的完整过程`,
        steps: [
          {
            description: "CPU在每条指令执行末尾检查是否有中断请求（检查中断信号线）",
            state: { phase: "check", note: "指令周期末尾的中断检测" },
          },
          {
            description: "关中断：CPU响应中断，自动关闭中断（防止处理过程中被新中断打断）",
            state: { phase: "disable-interrupt", note: "硬件自动完成" },
          },
          {
            description: "保存断点：将PC（程序计数器）和PSW压入系统栈",
            state: { phase: "save-pc-psw", note: "硬件自动完成" },
          },
          {
            description: "识别中断源：通过中断向量表或软件查询确定中断类型",
            state: { phase: "identify", note: "向量中断用硬件，非向量中断用软件" },
          },
          {
            description: "保存现场：将通用寄存器等压入栈（中断服务程序开头执行）",
            state: { phase: "save-context", note: "软件完成（中断服务程序负责）" },
          },
          {
            description: "开中断（可选）：允许更高优先级的中断嵌套",
            state: { phase: "enable-interrupt", note: "实现中断嵌套" },
          },
          {
            description: "执行中断服务程序：处理中断事件",
            state: { phase: "service", note: "具体处理逻辑" },
          },
          {
            description: "关中断 → 恢复现场 → 开中断 → 中断返回（IRET）",
            state: { phase: "return", note: "恢复PC和PSW，回到被中断的程序" },
          },
        ],
      },
    ],
    exercises: [
      {
        id: "interrupt-ex1",
        title: "中断与异常辨析",
        description: "判断以下事件属于中断还是异常，并说明具体类型：\n(1) 用户程序执行除法指令时除数为0\n(2) 打印机完成打印任务\n(3) 用户程序执行系统调用\n(4) 访问的页面不在内存中\n(5) 定时器到时",
        difficulty: "easy" as const,
        hints: ["判断事件来源是CPU内部还是外部", "异常中区分fault/trap/abort"],
        referenceSolution: "(1) 异常-故障(Fault)：除零错误，来自CPU内部，可能终止进程\n(2) 中断(外中断)：IO中断，来自CPU外部，异步事件\n(3) 异常-自陷(Trap)：系统调用通过陷入指令触发，有意为之\n(4) 异常-故障(Fault)：缺页异常，可恢复，处理后重新执行访存指令\n(5) 中断(外中断)：时钟中断，来自CPU外部的定时器",
      },
    ],
  },
  {
    id: "os-structure-boot",
    title: "操作系统结构与引导",
    brief: "宏内核/微内核/外核的设计思想，系统引导过程，虚拟机概念",
    keyTakeaways: [
      "宏内核：所有OS功能在内核态运行，性能高但可靠性差（如Linux）",
      "微内核：仅保留最基本功能在内核，其余作为用户态服务，可靠但性能有开销（如Minix）",
      "外核：直接将硬件资源分配给应用，最小化抽象（研究型OS）",
      "系统引导：BIOS/UEFI → MBR/GPT → 引导程序 → OS内核",
      "虚拟机：在一台物理机上模拟多台独立计算机，VMM管理硬件资源分配",
    ],
    commonMistakes: [
      "混淆微内核和宏内核的性能特点：微内核因频繁的用户态/内核态切换性能较低",
      "认为虚拟机只能用软件模拟：现代CPU有硬件虚拟化支持（VT-x/AMD-V）",
    ],
    sections: [
      {
        id: "os-arch",
        title: "操作系统体系结构",
        type: "concept",
        content: `## 宏内核（Monolithic Kernel）

把OS的所有功能（进程管理、内存管理、文件系统、设备驱动等）都塞进**内核态**，共享同一地址空间。像一个巨型公司把所有部门放在同一层楼——沟通快（直接喊一嗓子），但一个部门着火整层楼都遭殃。

- 优点：模块间直接函数调用，**性能高**（无需跨地址空间通信）
- 缺点：任何模块的bug都可能导致整个系统崩溃，**可靠性差**
- 代表：Linux、Windows NT内核

## 微内核（Microkernel）

内核只保留**最核心的三件事**：
- 进程间通信（IPC）
- 基本调度
- 底层内存管理

其他功能（文件系统、设备驱动、网络协议栈）全部搬到**用户态**作为独立进程运行。像把公司各部门分散到不同楼层，通过电话（IPC）沟通——一个部门出问题不影响其他部门，但打电话比喊一嗓子慢。

- 优点：**可靠性高**（一个服务崩溃不影响内核），易于扩展和移植
- 缺点：频繁的IPC和用户态/内核态切换带来**性能开销**（一次文件读取可能需要多次模式切换）
- 代表：Minix、QNX、seL4

## 外核（Exokernel）

核心思想：**OS不要替应用做决定**。传统OS提供"虚拟内存""文件系统"等抽象，但这些抽象不一定适合所有应用（比如数据库想自己管理磁盘布局）。

外核的做法：
- OS只负责**安全地分配和隔离**硬件资源（你用你的内存页，我用我的）
- 应用程序拿到原始硬件资源后，**自己决定怎么用**
- 相当于房东只管分房间，装修随你

优缺点：
- 优点：应用可以针对性优化，消除不必要的抽象开销
- 缺点：编程极其复杂（每个应用都要自己实现"文件系统"等功能）
- 主要存在于学术研究领域

## 对比

| 特性 | 宏内核 | 微内核 | 外核 |
|------|--------|--------|------|
| 内核态功能 | 全部 | 最少 | 极少 |
| 性能 | 高 | 较低 | 最高（理论） |
| 可靠性 | 低 | 高 | 中 |
| 可扩展性 | 差 | 好 | 好 |
| 复杂度 | 内核复杂 | 内核简单 | 应用复杂 |`,
      },
      {
        id: "os-boot",
        title: "系统引导过程",
        type: "detail",
        content: `## 引导过程（Boot Process）

计算机从加电到OS运行的过程：

### 1. 加电自检（POST）

CPU复位后从固定地址（如0xFFFF0）开始执行BIOS/UEFI固件代码。
- 检测硬件（内存、显卡、键盘等）
- 初始化基本硬件

### 2. 读取引导程序

**传统BIOS方式**：
- 读取磁盘第一个扇区（MBR，512字节）
- MBR包含引导代码(446B) + 分区表(64B) + 结束标志(2B)
- 引导代码找到活动分区，加载其引导扇区

**UEFI方式**：
- 直接从ESP（EFI System Partition）加载引导程序
- 支持GPT分区表，突破2TB限制
- 支持安全启动（Secure Boot）

### 3. 加载OS内核

引导程序（如GRUB）：
- 将OS内核从磁盘加载到内存
- 设置内核运行环境（保护模式、页表等）
- 跳转到内核入口点

### 4. OS初始化

- 初始化内核数据结构
- 检测和初始化设备驱动
- 创建第一个用户进程（如Linux的init/systemd）
- 进入正常运行状态`,
      },
      {
        id: "os-vm",
        title: "虚拟机",
        type: "detail",
        content: `## 虚拟机概念

虚拟机（Virtual Machine）：在一台物理机上模拟出多台逻辑计算机，每台虚拟机运行独立的OS。

**虚拟机监视器（VMM/Hypervisor）**：管理物理资源，为每个虚拟机提供虚拟硬件环境。

## 两种虚拟化类型

### Type 1（裸金属型）

VMM直接运行在硬件上，不需要宿主OS。

\`\`\`
  VM1(OS1)  VM2(OS2)  VM3(OS3)
  ─────────────────────────────
       VMM (Hypervisor)
  ─────────────────────────────
           硬件
\`\`\`

代表：VMware ESXi、Xen、KVM

### Type 2（宿主型）

VMM运行在宿主OS之上，作为一个应用程序。

\`\`\`
  VM1(OS1)  VM2(OS2)
  ─────────────────────
       VMM (应用程序)
  ─────────────────────
       宿主 OS
  ─────────────────────
       硬件
\`\`\`

代表：VMware Workstation、VirtualBox

## 硬件虚拟化支持

现代CPU提供硬件辅助虚拟化：
- Intel VT-x / AMD-V：增加新的CPU运行模式
- 二级地址转换（EPT/NPT）：减少内存虚拟化开销
- I/O虚拟化（VT-d）：设备直通

## 虚拟机的优势

- **隔离性**：虚拟机之间互不影响
- **安全性**：恶意软件被限制在虚拟机内
- **灵活性**：快速创建、迁移、快照
- **资源利用率**：多个轻负载服务共享物理机`,
      },
    ],
    exercises: [
      {
        id: "os-struct-ex1",
        title: "OS结构对比",
        description: "回答以下问题：\n(1) 微内核相比宏内核的主要优缺点是什么？\n(2) 简述BIOS引导方式下从加电到OS内核开始执行的主要步骤\n(3) Type 1和Type 2虚拟机监视器的区别是什么？各举一个例子",
        difficulty: "medium",
        hints: [
          "微内核的核心设计是将大部分功能移到用户态",
          "引导过程：POST → MBR → 引导程序 → 内核",
          "Type 1直接在硬件上运行",
        ],
        referenceSolution: `(1) 微内核优点：可靠性高（服务崩溃不影响内核）、可扩展性好、易于移植。缺点：性能较低，因为功能模块在用户态运行，模块间通信需要频繁的IPC和用户态/内核态切换，开销大。

(2) 步骤：①加电，CPU从固定地址执行BIOS代码 ②POST自检硬件 ③BIOS读取MBR（磁盘第一个扇区512字节）④MBR中的引导代码找到活动分区 ⑤加载活动分区的引导扇区 ⑥引导程序将OS内核加载到内存 ⑦跳转到内核入口，OS开始初始化

(3) Type 1（裸金属型）：VMM直接运行在硬件上，不需要宿主OS，性能好。例：VMware ESXi、Xen。Type 2（宿主型）：VMM作为宿主OS上的应用程序运行，部署简单但性能有额外开销。例：VirtualBox、VMware Workstation。`,
      },
    ],
    relatedLessons: ["os-concepts", "interrupt-exception"],
  },
];

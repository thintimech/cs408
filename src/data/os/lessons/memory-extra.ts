import { Lesson } from "@/types";

export const memoryExtraLessons: Lesson[] = [
  {
    id: "segment-paging",
    title: "段页式存储管理",
    brief: "结合分段和分页的优点，先分段再分页，是现代操作系统的主流方案",
    analogy: "段页式像大型图书馆——先按学科分区（段），每个学科区内再按统一编号的书架格子存放（页）。找书时先确定学科区，再找具体格子。",
    prerequisites: ["分页存储管理", "分段存储管理"],
    commonMistakes: [
      "段页式的段内地址不是直接的偏移——需要进一步拆分为页号和页内偏移",
      "段页式需要三次访存不是两次——段表→页表→目标数据",
      "段页式中段的大小不是固定的——每个段可以有不同数量的页",
    ],
    memoryAids: [
      "段页式地址 = 段号 + 页号 + 偏移（三段式）",
      "访存次数：无TLB=3次，有TLB命中=1次",
      "段页式 = 段的逻辑优势 + 页的内存管理优势",
    ],
    keyTakeaways: [
      "段页式 = 先按逻辑分段，再对每段分页",
      "逻辑地址结构：段号 + 段内页号 + 页内偏移",
      "每个进程一个段表，每个段一个页表",
      "地址转换需要三次访存（段表→页表→内存），用TLB加速",
      "兼具分段的逻辑清晰和分页的内存利用率高",
    ],
    relatedLessons: ["paging", "memory-basics"],
    sections: [
      {
        id: "motivation",
        title: "为什么需要段页式",
        type: "motivation",
        content: `**分页**的优点：内存利用率高（无外部碎片），缺点：不便于信息共享和保护（页是物理划分，无逻辑含义）。

**分段**的优点：便于共享和保护（段有逻辑含义），缺点：会产生外部碎片。

**段页式**结合两者：
- 对用户：按逻辑分段（代码段、数据段、栈段）
- 对系统：每段内部再分页

这样既保留了段的逻辑意义（便于共享保护），又消除了外部碎片（页式管理）。`,
      },
      {
        id: "structure",
        title: "段页式的地址结构",
        type: "concept",
        content: `逻辑地址由三部分组成：

| 段号 s | 段内页号 p | 页内偏移 d |
|--------|-----------|-----------|
| 确定哪个段 | 确定段内哪一页 | 确定页内位置 |

**数据结构**：
- **段表**：每个进程一个，每项包含（段号、页表长度、页表起始地址）
- **页表**：每个段一个，每项包含（页号、物理块号）

段表寄存器存放当前进程段表的起始地址和段表长度。`,
      },
      {
        id: "translation",
        title: "【算法】地址转换过程",
        type: "detail",
        content: `地址转换步骤：

1. 从逻辑地址中取出**段号s、页号p、偏移d**
2. 用段号查**段表**，得到该段的页表起始地址和页表长度
3. 检查页号是否越界（p < 页表长度），越界则产生中断
4. 用页号查该段的**页表**，得到物理块号b
5. 物理地址 = b × 页面大小 + d

**访存次数**：不加TLB时需要**3次访存**（访问段表 + 访问页表 + 访问目标单元）。

加入TLB后，命中时只需1次访存（TLB同时匹配段号+页号）。`,
      },
      {
        id: "walkthrough",
        title: "地址转换演示",
        type: "walkthrough",
        content: `设页面大小为1KB(1024B)，逻辑地址为 (段号=1, 段内地址=2500)`,
        steps: [
          {
            description: "分解地址：段号s=1，段内地址2500。页号p=⌊2500/1024⌋=2，页内偏移d=2500 mod 1024=452",
            state: { segment: 1, page: 2, offset: 452, phase: "decompose" },
          },
          {
            description: "查段表：段号1对应的页表起始地址为8000，页表长度为4（即该段有4页）",
            state: { segment: 1, pageTableAddr: 8000, pageTableLen: 4, phase: "segment-table" },
          },
          {
            description: "越界检查：页号2 < 页表长度4，未越界，继续",
            state: { page: 2, limit: 4, check: "pass", phase: "bound-check" },
          },
          {
            description: "查页表：页表地址8000，第2项对应物理块号为6",
            state: { page: 2, frameNumber: 6, phase: "page-table" },
          },
          {
            description: "计算物理地址：6 × 1024 + 452 = 6596",
            state: { frame: 6, offset: 452, physicalAddress: 6596, phase: "result" },
          },
        ],
      },
      {
        id: "comparison",
        title: "三种存储管理方式对比",
        type: "comparison",
        content: `| | 分页 | 分段 | 段页式 |
|--|------|------|--------|
| 划分依据 | 物理（固定大小） | 逻辑（按功能） | 先逻辑后物理 |
| 地址结构 | 页号+偏移 | 段号+段内偏移 | 段号+页号+偏移 |
| 内部碎片 | 有（最后一页） | 无 | 有（最后一页） |
| 外部碎片 | 无 | 有 | 无 |
| 共享保护 | 不便 | 方便 | 方便 |
| 访存次数 | 2次(+TLB→1次) | 2次(+TLB→1次) | 3次(+TLB→1次) |
| 典型应用 | Linux | 早期系统 | Intel x86 |`,
      },
    ],
    exercises: [
      {
        id: "sp-ex1",
        title: "段页式地址转换",
        description: "某段页式系统，页面大小4KB，某进程逻辑地址为(段号=2, 段内地址=8196)。求页号和页内偏移。若该页对应物理块号为10，求物理地址。",
        difficulty: "medium" as const,
        hints: ["页号=⌊段内地址/页面大小⌋", "页内偏移=段内地址 mod 页面大小"],
        referenceSolution: "页号=⌊8196/4096⌋=2，偏移=8196 mod 4096=4。物理地址=10×4096+4=40964",
      },
    ],
  },
  {
    id: "segmentation",
    title: "分段存储管理",
    brief: "按程序逻辑结构分段管理内存，便于共享和保护",
    analogy: "分段像把一本书按章节拆开存放——每章（段）可以独立存放在书架的不同位置，长度不同也没关系。你只需要知道「第3章在哪个书架的哪个位置」就能找到。好处是可以把某一章借给别人看（共享），也可以锁住某一章不让人看（保护）。",
    prerequisites: ["内存管理基础", "分页存储管理"],
    commonMistakes: [
      "分段的段长是不固定的——不像分页的页面大小固定，每个段可以有不同长度",
      "分段会产生外部碎片——因为段长不等，内存中会出现无法利用的小空隙",
      "段号是由程序员/编译器决定的——不像页号是系统自动划分的",
      "分段的地址是二维的——需要(段号, 段内偏移)两个分量才能确定地址",
    ],
    memoryAids: [
      "分段 vs 分页：段是逻辑划分（用户可见），页是物理划分（用户透明）",
      "段表项：段号 + 段长 + 基址（比页表多了段长字段，因为段长不固定）",
      "分段优势：共享（共享同一个段）、保护（按段设置权限）、动态链接",
      "分段劣势：外部碎片（需要紧凑/拼接来解决）",
    ],
    keyTakeaways: [
      "分段按程序的逻辑结构划分（代码段、数据段、栈段等）",
      "逻辑地址 = (段号, 段内偏移)，是二维地址",
      "段表项包含段长和基址，地址转换时需要越界检查",
      "分段便于共享和保护（以段为单位设置权限）",
      "分段会产生外部碎片，分页不会",
      "分段对用户可见，分页对用户透明",
    ],
    relatedLessons: ["paging", "segment-paging", "memory-basics"],
    sections: [
      {
        id: "seg-concept",
        title: "分段的基本概念",
        type: "concept",
        content: `## 为什么要分段

程序由若干逻辑段组成：主程序段、子程序段、数据段、栈段等。每个段有完整的逻辑意义。

分页是从系统管理角度出发（物理划分），分段是从用户角度出发（逻辑划分）。

## 段的特点

- 每个段有段名（编译后变为段号）
- 每个段从0开始编址
- 段的长度不固定（由逻辑信息的长度决定）
- 各段在内存中可以不连续存放

## 逻辑地址结构

| 段号 s | 段内偏移 d |
|--------|-----------|
| 确定哪个段 | 段内的位置 |

地址是**二维**的：需要段号和偏移两个独立分量。`,
      },
      {
        id: "seg-translation",
        title: "【算法】分段地址转换",
        type: "detail",
        content: `## 段表

每个进程一个段表，每个段表项包含：

| 段号 | 段长 | 基址(段在内存中的起始地址) |
|------|------|--------------------------|

## 地址转换过程

1. 从逻辑地址取出**段号s**和**段内偏移d**
2. 查段表，找到段号s对应的段表项
3. **越界检查**：若 d ≥ 段长，产生越界中断
4. 物理地址 = 基址 + d

## 与分页的对比

| | 分页 | 分段 |
|--|------|------|
| 划分方式 | 系统自动，固定大小 | 用户/编译器，大小不等 |
| 地址维度 | 一维（线性地址自动拆分） | 二维（段号+偏移独立给出） |
| 内部碎片 | 有（最后一页） | 无 |
| 外部碎片 | 无 | 有 |
| 共享保护 | 不便（页无逻辑含义） | 方便（段有逻辑含义） |
| 用户可见性 | 透明 | 可见 |`,
      },
      {
        id: "seg-sharing",
        title: "段的共享与保护",
        type: "detail",
        content: `## 段的共享

多个进程可以共享同一个段（如共享库代码段）：
- 各进程段表中对应段表项指向**同一物理内存区域**
- 共享段通常是**纯代码段**（可重入代码），不含可修改的数据

## 段的保护

两种保护机制：

1. **越界保护**：段内偏移 ≥ 段长时产生中断
2. **权限保护**：段表项中设置访问权限位（只读/可执行/可读写）

## 可重入代码(Pure Code)

- 代码段中不包含任何可修改的数据
- 所有可修改的数据放在各进程私有的数据段中
- 这样多个进程可以安全地共享同一份代码

## 分段的缺点

- **外部碎片**：段长不等，内存中产生很多小空隙
- 解决方法：紧凑（Compaction）——移动段使空闲空间合并
- 紧凑的代价：需要移动大量数据，开销大

> 这就是为什么需要段页式——结合分段的逻辑优势和分页的内存管理优势。`,
      },
    ],
    exercises: [
      {
        id: "seg-ex1",
        title: "分段地址转换",
        description: "某分段系统中，段表如下：\n\n| 段号 | 段长 | 基址 |\n|------|------|------|\n| 0 | 500 | 1000 |\n| 1 | 200 | 3000 |\n| 2 | 800 | 5000 |\n\n求逻辑地址(1, 150)和(2, 900)对应的物理地址。",
        difficulty: "easy" as const,
        hints: ["先检查段内偏移是否越界", "物理地址 = 基址 + 偏移"],
        referenceSolution: "(1, 150)：段号1，段长200，偏移150<200未越界。物理地址=3000+150=3150。\n\n(2, 900)：段号2，段长800，偏移900≥800，越界！产生越界中断，地址转换失败。",
      },
    ],
  },
  {
    id: "virtual-memory-advanced",
    title: "虚拟内存高级主题",
    brief: "抖动与工作集模型、内存分配策略、页面分配与置换策略的组合",
    analogy: "抖动像一个人同时做太多事——不停地在任务间切换，每次切换都要重新找资料，结果什么都没做完。工作集就是「当前手头真正需要的那几份资料」，只要桌面放得下这些资料，工作就能顺畅进行。",
    prerequisites: ["页面置换算法", "请求分页管理"],
    commonMistakes: [
      "抖动不是缺页——缺页是正常现象，抖动是缺页率高到系统几乎无法工作",
      "工作集大小不是固定的——随程序执行阶段变化（局部性变化）",
      "固定分配不等于性能差——如果分配的帧数足够覆盖工作集，性能很好",
      "全局置换不一定比局部置换好——全局置换可能导致一个进程抢占另一个进程的帧",
    ],
    memoryAids: [
      "抖动原因：进程太多 → 每个进程帧太少 → 缺页频繁 → CPU利用率反而下降",
      "工作集 W(t,Δ) = 过去Δ时间内访问的页面集合",
      "分配策略2×2：固定/可变分配 × 局部/全局置换（固定+全局不存在）",
    ],
    keyTakeaways: [
      "抖动(Thrashing)：进程频繁缺页，CPU利用率急剧下降",
      "工作集模型：跟踪进程近期活跃页面集，保证分配帧数≥工作集大小",
      "驻留集：进程实际驻留在内存中的页面集合",
      "固定分配+局部置换：简单但不灵活",
      "可变分配+全局置换：灵活但可能互相影响",
      "可变分配+局部置换：最灵活，根据缺页率动态调整",
      "预防抖动：控制多道程度，使用工作集策略或缺页率策略(PFF)",
    ],
    relatedLessons: ["page-replacement", "paging"],
    sections: [
      {
        id: "thrashing",
        title: "抖动（颠簸）",
        type: "concept",
        content: `## 什么是抖动

当进程分配的物理帧数不足以容纳其工作集时，会频繁发生缺页中断。进程大部分时间都在等待页面调入，CPU利用率急剧下降。

## 抖动的产生过程

1. 多道程度增加 → 每个进程分到的帧数减少
2. 帧数不足 → 缺页率上升
3. 缺页处理需要I/O → 进程阻塞增多 → CPU利用率下降
4. 操作系统误以为需要提高多道程度 → 引入更多进程
5. 恶性循环：更多进程 → 每个进程帧更少 → 缺页更频繁

## CPU利用率与多道程度的关系

\`\`\`
CPU利用率
  ↑      ╱‾‾‾╲
  |    ╱       ╲  ← 抖动开始
  |  ╱           ╲
  |╱               ╲___
  +————————————————————→ 多道程度
\`\`\`

**关键点**：存在一个最优多道程度，超过后CPU利用率不升反降。`,
      },
      {
        id: "working-set",
        title: "工作集模型",
        type: "concept",
        content: `## 工作集定义

**工作集 W(t, Δ)**：在时刻 t 之前的 Δ 个页面引用中，进程实际访问的不同页面的集合。

- Δ 称为**工作集窗口**（window size）
- |W(t, Δ)| 为工作集大小

## 示例

页面引用串：...2, 6, 1, 5, 7, 7, 7, 7, 5, 1...

若 Δ = 10，则在当前时刻 t 的工作集 W(t,10) = {1, 2, 5, 6, 7}，大小为 5。

## 工作集策略

- 为每个进程分配的帧数 ≥ 其工作集大小
- 若所有进程工作集之和 > 可用帧总数，则挂起某些进程（降低多道程度）
- Δ 的选择很关键：太小会遗漏活跃页面，太大会包含非活跃页面

## 驻留集 vs 工作集

| 概念 | 含义 |
|------|------|
| 工作集 | 进程近期活跃的页面集合（理论概念） |
| 驻留集 | 进程实际在内存中的页面集合（实际分配） |

**目标**：驻留集 ⊇ 工作集（驻留集覆盖工作集）`,
      },
      {
        id: "allocation-strategies",
        title: "【重点】页面分配与置换策略",
        type: "detail",
        content: `## 分配策略

**固定分配**：进程创建时分配固定数量的帧，运行期间不变。
- 问题：如何确定分配多少？太少→缺页多，太多→浪费

**可变分配**：根据运行情况动态调整分配的帧数。
- 缺页率高→多分配帧，缺页率低→回收部分帧

## 置换策略

**局部置换**：缺页时只在本进程的帧中选择淘汰页。
- 不影响其他进程，但可能本进程帧不够用

**全局置换**：缺页时从所有进程的帧中选择淘汰页。
- 灵活，但可能"抢"其他进程的帧

## 组合方式（考研重点）

| 分配 | 置换 | 说明 | 特点 |
|------|------|------|------|
| 固定 | 局部 | 帧数固定，只换自己的 | 简单，但不灵活 |
| 固定 | 全局 | ❌ 不存在 | 固定分配不允许从别人那拿帧 |
| 可变 | 局部 | 帧数可调，只换自己的 | 最灵活，根据PFF调整 |
| 可变 | 全局 | 帧数可调，可换任何人的 | 实现简单，但可能互相干扰 |

> **固定分配+全局置换**不存在：固定分配意味着帧数不变，而全局置换会改变各进程的帧数，矛盾。`,
      },
      {
        id: "pff",
        title: "缺页率策略(PFF)",
        type: "detail",
        content: `## 页面故障频率策略 (Page Fault Frequency)

直接监控每个进程的缺页率，动态调整帧分配：

- 缺页率 > 上限阈值 → 为该进程增加帧
- 缺页率 < 下限阈值 → 从该进程回收帧
- 缺页率在上下限之间 → 保持不变

## 与工作集策略的对比

| | 工作集策略 | PFF策略 |
|--|-----------|---------|
| 监控对象 | 页面引用串 | 缺页率 |
| 调整依据 | 工作集大小变化 | 缺页率超出阈值 |
| 实现难度 | 较高（需维护引用历史） | 较低（只需统计缺页次数） |
| 响应速度 | 较慢 | 较快 |

## 预防抖动的方法总结

1. **工作集策略**：保证帧数≥工作集大小
2. **PFF策略**：根据缺页率动态调整
3. **挂起进程**：多道程度过高时挂起部分进程到外存
4. **L=S准则**：缺页间隔平均时间(L) ≈ 缺页处理时间(S) 时系统效率最优`,
      },
    ],
    exercises: [
      {
        id: "vm-adv-ex1",
        title: "工作集计算",
        description: "某进程的页面引用串为：1,2,3,2,4,5,2,3,2,1,5,4。工作集窗口Δ=5。求在第8次引用后（即引用完第8个页面'3'后）的工作集和工作集大小。",
        difficulty: "medium" as const,
        hints: ["工作集是过去Δ次引用中出现的不同页面", "第8次引用后，往前看5次引用"],
        referenceSolution: "第8次引用后，往前5次引用为：4,5,2,3,2。出现的不同页面为{2,3,4,5}，工作集大小=4。",
      },
      {
        id: "vm-adv-ex2",
        title: "分配置换策略判断",
        description: "判断以下说法的正误：\n(1) 固定分配+全局置换是一种有效的策略组合\n(2) 可变分配+局部置换可以根据缺页率调整帧数\n(3) 抖动一定发生在多道程度很高的时候\n(4) 工作集越大说明程序局部性越好",
        difficulty: "medium" as const,
        hints: ["固定分配的含义是帧数不变", "局部性好意味着集中访问少量页面"],
        referenceSolution: "(1) 错误。固定分配+全局置换是矛盾的，不存在这种组合。\n(2) 正确。可变分配允许动态调整帧数，局部置换只在本进程内选择淘汰页，结合PFF策略可根据缺页率增减帧。\n(3) 错误。抖动通常发生在多道程度过高时，但如果单个进程的工作集远大于分配的帧数，即使多道程度不高也可能抖动。\n(4) 错误。工作集越大说明程序访问的页面越分散，局部性越差。局部性好的程序工作集小。",
      },
    ],
  },
];

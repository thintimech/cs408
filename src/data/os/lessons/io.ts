import { Lesson } from "@/types";

export const ioLessons: Lesson[] = [
  {
    id: "io-control",
    title: "IO控制方式",
    brief: "理解四种IO控制方式及其演进过程",
    analogy: "IO控制方式的演进像点外卖——程序查询是你站在门口一直等外卖小哥（CPU空转），中断是你回去干别的事等电话通知，DMA是外卖小哥自己把餐放桌上不用你动手，通道是你雇了个管家全权负责所有外卖事务。",
    prerequisites: ["中断的基本概念", "CPU与外设的速度差异"],
    commonMistakes: [
      "中断方式CPU不是完全不管IO——每传送一个字/字节都要中断一次CPU",
      "DMA传送期间CPU不是完全空闲——CPU可以执行程序，但不能访问被DMA占用的总线",
      "DMA传送的是一个数据块——不是一个字节，这是它比中断高效的关键",
      "通道是独立的处理器——有自己的指令系统（通道程序），不是简单的硬件控制器",
    ],
    memoryAids: [
      "演进路线：程序查询→中断→DMA→通道（CPU介入程度递减）",
      "中断：以字节/字为单位，每次传送都中断CPU",
      "DMA：以块为单位，传送完一整块才中断CPU",
      "DMA三种方式：停止CPU、周期挪用、交替分时",
    ],
    sections: [
      {
        id: "io-evolution",
        title: "IO控制方式的演进",
        type: "motivation",
        content: `## IO控制的目标

减少CPU对IO操作的干预，提高CPU与IO设备的并行度。

IO控制方式经历了从CPU全程参与到几乎不参与的演进过程。`,
      },
      {
        id: "io-methods",
        title: "四种IO控制方式",
        type: "comparison",
        content: `## 1. 程序直接控制方式(轮询/忙等待)

CPU不断查询IO设备状态，直到IO完成。

- CPU利用率：极低
- 每次传输单位：字/字节
- 特点：CPU全程等待，简单但效率最差

## 2. 中断驱动方式

CPU发出IO命令后继续执行其他任务，IO完成时通过中断通知CPU。

- CPU利用率：提高
- 每次传输单位：字/字节
- 特点：CPU在IO期间可做其他事，但每个字都要中断一次

## 3. DMA方式(直接存储器访问)

DMA控制器直接在IO设备和内存之间传输数据，无需CPU逐字干预。

- CPU利用率：进一步提高
- 每次传输单位：块
- 特点：以块为单位传输，传输完成才中断CPU一次

## 4. 通道方式

通道是专用IO处理器，可执行通道程序，独立完成复杂IO操作。

- CPU利用率：最高
- 每次传输单位：一组块
- 特点：CPU只需启动通道，通道自主完成全部IO

## 对比表

| 方式 | CPU干预程度 | 传输单位 | 数据流向 | 优缺点 |
|------|------------|----------|----------|--------|
| 程序直接控制 | 极高(全程) | 字 | 设备->CPU->内存 | 简单但CPU利用率极低 |
| 中断驱动 | 高(每字) | 字 | 设备->CPU->内存 | CPU可并行但中断频繁 |
| DMA | 低(每块) | 块 | 设备->内存 | 块传输，CPU介入少 |
| 通道 | 极低(每组) | 一组块 | 设备->内存 | 最高效但硬件成本高 |`,
      },
      {
        id: "dma-detail",
        title: "DMA工作原理",
        type: "detail",
        content: `## DMA控制器的组成

- **命令/状态寄存器(CR)**：存放CPU发来的IO命令和设备状态
- **内存地址寄存器(MAR)**：数据在内存中的起始地址
- **数据计数器(DC)**：要传输的字节数
- **数据寄存器(DR)**：暂存数据

## DMA传输过程

1. CPU设置DMA参数（内存地址、传输字节数、传输方向）
2. CPU启动DMA传输，继续执行其他任务
3. DMA控制器自主完成数据传输（设备<->内存）
4. 传输完成，DMA向CPU发出中断

## DMA与中断方式的区别

| 比较项 | 中断方式 | DMA方式 |
|--------|----------|---------|
| 中断时机 | 每传输一个字 | 传输完一个块 |
| 数据流向 | 设备->CPU->内存 | 设备->内存(直接) |
| CPU介入 | 每字都参与 | 只在开始和结束 |
| 响应时机 | 指令执行完 | 总线空闲时(周期窃取) |`,
      },
    ],
    exercises: [
      {
        id: "io-control-ex1",
        title: "IO控制方式选择",
        description: "分析以下场景应该使用哪种IO控制方式，并说明理由：\n(1) 键盘输入单个字符\n(2) 磁盘读取一个4KB的文件块\n(3) 大型机批量处理磁带数据",
        difficulty: "easy",
        hints: [
          "考虑数据量大小和实时性要求",
          "键盘输入频率低，数据量小",
          "磁盘传输以块为单位",
        ],
        referenceSolution: `(1) 键盘输入单个字符 -> 中断驱动方式
理由：键盘输入频率低（人的打字速度有限），每次只有一个字符，使用中断方式即可。CPU在等待按键期间可以做其他事，按键时中断通知CPU。不需要DMA（数据量太小，DMA初始化开销不值得）。

(2) 磁盘读取4KB文件块 -> DMA方式
理由：磁盘传输以块为单位，数据量较大(4KB=4096字节)。如果用中断方式，需要中断4096次，开销太大。DMA可以一次性传输整个块，只在完成时中断一次CPU。

(3) 大型机批量处理磁带数据 -> 通道方式
理由：批量数据处理涉及大量连续IO操作，通道可以执行预编好的通道程序，自主完成一系列IO操作（如读多个块、格式转换等），最大程度解放CPU。`,
      },
    ],
    keyTakeaways: [
      "IO控制方式演进：程序直接控制 -> 中断 -> DMA -> 通道",
      "演进方向：减少CPU干预，提高CPU与IO并行度",
      "DMA以块为单位传输，数据不经过CPU直接到内存",
      "通道是专用IO处理器，可执行通道程序",
      "选择依据：数据量、传输频率、系统规模",
    ],
    relatedLessons: ["io-management"],
  },
  {
    id: "io-management",
    title: "IO管理技术",
    brief: "掌握缓冲技术、设备分配和SPOOLing技术",
    analogy: "缓冲像快递驿站——快递员（IO设备）把包裹放驿站（缓冲区），你（CPU）有空再去取，双方不用同时在场。SPOOLing像共享打印机——大家把打印任务提交到队列，打印机按顺序处理，每个人感觉自己独占了一台打印机。",
    prerequisites: ["IO控制方式", "进程的基本概念"],
    commonMistakes: [
      "单缓冲的处理时间不是简单的T+C——要考虑缓冲区满才能取走，取走后才能再装入",
      "双缓冲不是速度翻倍——是让输入和处理可以并行，总时间取max(T,C+M)",
      "SPOOLing不是真正的脱机——是用软件模拟脱机输入/输出（假脱机）",
      "设备独立性不是说程序不需要设备——是说程序用逻辑设备名，OS负责映射到物理设备",
    ],
    memoryAids: [
      "单缓冲：max(T,C) + M（T=输入时间，C=处理时间，M=传送时间）",
      "双缓冲：max(T, C+M)，实现了输入与处理的并行",
      "SPOOLing三部分：输入井+输出井+输入/输出进程",
    ],
    sections: [
      {
        id: "buffering",
        title: "缓冲技术",
        type: "concept",
        content: `## 为什么需要缓冲

1. 缓和CPU与IO设备速度不匹配的矛盾
2. 减少CPU中断次数
3. 提高CPU与IO设备的并行性

## 缓冲类型

### 单缓冲
- 一个缓冲区在CPU和设备之间
- 设备输入到缓冲区，CPU从缓冲区取数据
- 处理一块数据的时间：max(C, T) + M
  - T：设备传输时间
  - M：缓冲区到用户区的传送时间
  - C：CPU处理时间

### 双缓冲
- 两个缓冲区交替使用
- 设备填一个缓冲区时，CPU处理另一个
- 处理一块数据的时间：max(C+M, T)
- 实现了CPU和IO的真正并行

### 循环缓冲
- 多个缓冲区组成循环队列
- 适合IO速度波动的情况

### 缓冲池
- 系统中的公共缓冲区集合
- 按使用状态分为：空缓冲队列、输入队列、输出队列
- 多个进程共享，提高缓冲区利用率`,
      },
      {
        id: "device-allocation",
        title: "设备分配",
        type: "detail",
        content: `## 设备分类

- **独占设备**：一次只能分配给一个进程（如打印机）
- **共享设备**：可同时分配给多个进程（如磁盘）
- **虚拟设备**：通过SPOOLing将独占设备改造为共享设备

## 设备分配的数据结构

1. **设备控制表(DCT)**：每个设备一个，记录设备状态、等待队列等
2. **控制器控制表(COCT)**：每个控制器一个
3. **通道控制表(CHCT)**：每个通道一个
4. **系统设备表(SDT)**：记录系统中所有设备

## 设备分配策略

- **静态分配**：进程运行前分配所有设备，运行完归还。安全但利用率低。
- **动态分配**：运行中按需申请。利用率高但可能死锁。`,
      },
      {
        id: "spooling",
        title: "SPOOLing技术",
        type: "concept",
        content: `## SPOOLing (Simultaneous Peripheral Operations On-Line)

假脱机技术：用软件方式将独占设备改造为共享设备。

## 组成

1. **输入井/输出井**：磁盘上的存储区域，模拟脱机IO的磁带
2. **输入缓冲区/输出缓冲区**：内存中的缓冲区
3. **输入进程/输出进程**：模拟脱机IO的外围机

## 工作原理（以打印为例）

1. 用户进程请求打印时，SPOOLing系统：
   - 在输出井中申请一块空间
   - 将数据送入输出井
   - 在输出队列中登记一个请求
2. 用户进程认为打印已完成（实际只是写入磁盘）
3. 输出进程按队列顺序将数据从输出井送到打印机

## 特点

- 提高了IO速度（对用户进程而言）
- 将独占设备改造为共享设备
- 实现了虚拟设备功能

## 典型应用

- 共享打印机（打印机本身是独占设备）
- 多个进程可"同时"打印，实际由SPOOLing排队处理`,
      },
    ],
    exercises: [
      {
        id: "io-mgmt-ex1",
        title: "缓冲区时间计算",
        description: "某系统对磁盘进行数据读取，每块数据的传输时间T=100us，缓冲区送到用户区时间M=10us，CPU处理时间C=50us。分别计算使用单缓冲和双缓冲时，处理10块数据的总时间。",
        difficulty: "medium",
        hints: [
          "单缓冲：每块时间 = max(C, T) + M",
          "双缓冲：每块时间 = max(C+M, T)",
          "注意最后一块数据的处理",
        ],
        referenceSolution: `**单缓冲：**
每块处理时间 = max(C, T) + M = max(50, 100) + 10 = 110us
10块总时间 = 110 * 10 = 1100us

更精确的分析：
- 前9块：每块 max(T, C) + M = 110us
- 最后一块传输完后还需 M + C = 60us
- 总时间 = 9 * 110 + 100 + 10 + 50 = 990 + 160 = 1150us
(简化计算通常用 n * (max(C,T)+M) = 1100us)

**双缓冲：**
每块处理时间 = max(C+M, T) = max(50+10, 100) = 100us
10块总时间 = 100 * 10 = 1000us

更精确：
- 前9块：每块 max(T, C+M) = 100us
- 最后一块还需 C + M = 60us
- 总时间 = 9 * 100 + 100 + 60 = 1060us
(简化计算用 n * max(C+M, T) = 1000us)

双缓冲比单缓冲快约9%，因为实现了IO和CPU处理的并行。`,
      },
    ],
    keyTakeaways: [
      "缓冲技术缓和CPU与IO设备的速度差异",
      "单缓冲每块时间max(C,T)+M，双缓冲每块时间max(C+M,T)",
      "SPOOLing将独占设备改造为共享设备",
      "SPOOLing的核心是用磁盘空间换取设备共享能力",
    ],
    relatedLessons: ["io-control"],
  },
];

import { Lesson } from "@/types";

export const fileLessons: Lesson[] = [
  {
    id: "file-structure",
    title: "文件系统基础",
    brief: "理解文件的逻辑结构、目录结构和物理结构",
    analogy: "文件系统像图书馆管理——逻辑结构是书的内容组织方式（小说按章节、字典按字母），目录结构是图书馆的分类索引卡片柜，物理结构是书实际摆放在哪些书架的哪些格子里。",
    prerequisites: ["磁盘的基本结构", "操作系统基本概念"],
    commonMistakes: [
      "文件的逻辑结构和物理结构是独立的——同一个逻辑结构可以用不同物理结构存储",
      "索引分配不是索引文件——索引分配是物理结构（磁盘块的组织），索引文件是逻辑结构（记录的组织）",
      "连续分配的「连续」是磁盘块连续——不是文件内容在逻辑上连续",
      "FAT表是整个磁盘共享一张——不是每个文件一张",
    ],
    memoryAids: [
      "物理结构三种：连续（快但碎片）、链接（无碎片但慢）、索引（折中但费空间）",
      "目录演进：单级→两级→树形→无环图（共享）",
      "FCB = 文件控制块 = 文件的「身份证」（文件名+属性+磁盘位置）",
    ],
    sections: [
      {
        id: "logical-structure",
        title: "文件的逻辑结构",
        type: "concept",
        content: `## 文件的逻辑结构

从用户角度看文件的组织方式：

### 1. 顺序文件(Sequential File)
- 记录按某种顺序排列
- 顺序访问效率高，随机访问效率低
- 适合批处理

### 2. 索引文件(Indexed File)
- 为文件建立索引表，索引项包含关键字和指针
- 支持快速随机访问
- 需要额外空间存储索引表

### 3. 索引顺序文件(Indexed Sequential File)
- 将记录分组，组间有索引
- 组内顺序查找，组间索引查找
- 兼顾顺序和随机访问`,
      },
      {
        id: "directory-structure",
        title: "目录结构",
        type: "concept",
        content: `## 目录的功能

目录用于实现"按名存取"，记录文件名到物理位置的映射。

## 目录结构类型

### 1. 单级目录
- 整个系统一个目录表
- 简单但不允许重名，不适合多用户

### 2. 两级目录
- 主文件目录(MFD) + 用户文件目录(UFD)
- 不同用户可以有同名文件
- 不能对文件分类

### 3. 树形目录(多级目录)
- 层次结构，每个目录可包含文件和子目录
- 路径名唯一标识文件
- **绝对路径**：从根目录开始
- **相对路径**：从当前目录开始
- 现代操作系统普遍采用

### 4. 无环图目录
- 允许文件有多个父目录（共享文件）
- 通过链接(link)实现
- 需要处理删除时的引用计数问题`,
      },
      {
        id: "physical-structure",
        title: "文件的物理结构",
        type: "comparison",
        content: `## 文件在磁盘上的存储方式

### 1. 连续分配
- 文件占用连续的磁盘块
- 优点：顺序和随机访问都快
- 缺点：外部碎片，文件不能动态增长

### 2. 链接分配
- 每个磁盘块包含指向下一块的指针
- **隐式链接**：每块末尾存下一块地址
  - 优点：无外部碎片，文件可动态增长
  - 缺点：只能顺序访问，指针占空间
- **显式链接(FAT)**：文件分配表集中存放指针
  - 优点：支持随机访问（通过FAT表）
  - 缺点：FAT表需常驻内存

### 3. 索引分配
- 每个文件有一个索引块，存放所有磁盘块号
- 优点：支持随机访问，无外部碎片
- 缺点：索引块占空间
- 大文件解决方案：多级索引、混合索引

## 对比表

| 方式 | 顺序访问 | 随机访问 | 碎片 | 文件增长 |
|------|----------|----------|------|----------|
| 连续 | 快 | 快 | 外部碎片 | 困难 |
| 链接 | 可以 | 慢 | 无 | 容易 |
| 索引 | 可以 | 快 | 无 | 容易 |`,
      },
    ],
    exercises: [
      {
        id: "file-ex1",
        title: "文件物理结构分析",
        description: "某文件系统采用混合索引分配方式，每个文件的FCB中包含：10个直接地址项、1个一级间接地址项、1个二级间接地址项。磁盘块大小为1KB，每个地址项占4B。求文件的最大长度。",
        difficulty: "hard",
        hints: [
          "一个磁盘块能存放的地址项数 = 1024/4 = 256",
          "直接地址：10个块",
          "一级间接：一个索引块指向256个数据块",
          "二级间接：一个索引块指向256个一级索引块",
        ],
        referenceSolution: `每个磁盘块可存放地址项数 = 1KB / 4B = 256个

文件最大长度计算：
- 直接地址：10个块 = 10KB
- 一级间接：256个块 = 256KB
- 二级间接：256 * 256个块 = 65536KB = 64MB

最大文件长度 = 10KB + 256KB + 65536KB
             = 65802KB
             约等于 64.26MB

注意：如果还有三级间接，则可支持 256^3 = 16GB 的额外空间。`,
      },
    ],
    keyTakeaways: [
      "文件逻辑结构：顺序、索引、索引顺序",
      "目录结构从单级发展到树形，解决命名和分类问题",
      "连续分配快但有碎片，链接分配灵活但随机访问慢",
      "索引分配兼顾随机访问和动态增长",
      "混合索引是Unix/Linux文件系统的典型方案",
    ],
    relatedLessons: ["disk-scheduling"],
  },
  {
    id: "disk-scheduling",
    title: "磁盘调度算法",
    brief: "掌握FCFS、SSTF、SCAN、C-SCAN、LOOK等磁盘调度算法",
    sections: [
      {
        id: "disk-structure",
        title: "磁盘结构与访问时间",
        type: "concept",
        content: `## 磁盘访问时间

磁盘访问时间 = 寻道时间 + 旋转延迟 + 传输时间

- **寻道时间**：磁头移动到目标磁道的时间（最主要的优化目标）
- **旋转延迟**：等待目标扇区转到磁头下方（平均半圈）
- **传输时间**：数据传输时间（通常很短）

磁盘调度算法的目标：**最小化总寻道距离**`,
      },
      {
        id: "disk-algorithms",
        title: "磁盘调度算法演示",
        type: "walkthrough",
        content: `## 示例

磁盘请求队列(磁道号)：98, 183, 37, 122, 14, 124, 65, 67
当前磁头位置：53
磁道范围：0-199`,
        steps: [
          {
            description: "FCFS：按请求到达顺序服务。移动序列：53->98->183->37->122->14->124->65->67",
            state: { algorithm: "FCFS", sequence: [53, 98, 183, 37, 122, 14, 124, 65, 67], totalMovement: 640 },
          },
          {
            description: "SSTF(最短寻道时间优先)：每次选择离当前磁头最近的请求",
            state: { algorithm: "SSTF", sequence: [53, 65, 67, 37, 14, 98, 122, 124, 183], totalMovement: 236 },
          },
          {
            description: "SCAN(电梯算法)：磁头向一个方向移动，到达边界后反向。假设初始向外(增大方向)",
            state: { algorithm: "SCAN", sequence: [53, 65, 67, 98, 122, 124, 183, 199, 37, 14], totalMovement: 331, note: "到达199后反向" },
          },
          {
            description: "C-SCAN(循环扫描)：磁头只在一个方向上服务，到达边界后直接回到起点",
            state: { algorithm: "C-SCAN", sequence: [53, 65, 67, 98, 122, 124, 183, 199, 0, 14, 37], totalMovement: 382, note: "到199后跳回0" },
          },
          {
            description: "LOOK：类似SCAN但不到边界，只到最远请求处就反向",
            state: { algorithm: "LOOK", sequence: [53, 65, 67, 98, 122, 124, 183, 37, 14], totalMovement: 299, note: "到183后反向(不到199)" },
          },
        ],
      },
      {
        id: "disk-comparison",
        title: "算法对比",
        type: "comparison",
        content: `## 磁盘调度算法对比

| 算法 | 总寻道距离 | 优点 | 缺点 |
|------|-----------|------|------|
| FCFS | 640 | 公平简单 | 寻道距离大 |
| SSTF | 236 | 寻道距离小 | 可能饥饿(远处请求) |
| SCAN | 331 | 无饥饿 | 两端请求等待时间不均 |
| C-SCAN | 382 | 等待时间更均匀 | 比SCAN多一次空行程 |
| LOOK | 299 | 比SCAN少走到边界 | 实现稍复杂 |

## 选择建议

- 请求量小：FCFS即可
- 请求量大：SCAN或LOOK
- 对响应时间要求均匀：C-SCAN
- SSTF性能好但可能饥饿，实际中较少单独使用`,
      },
    ],
    exercises: [
      {
        id: "disk-ex1",
        title: "磁盘调度计算",
        description: "磁盘请求队列为：55, 58, 39, 18, 90, 160, 150, 38, 184。当前磁头在100号磁道，向磁道号增大方向移动。分别用SCAN和C-SCAN算法计算总寻道距离。",
        difficulty: "medium",
        hints: [
          "SCAN：先向增大方向服务，到边界后反向",
          "C-SCAN：只向增大方向服务，到边界后回到最小处",
          "LOOK变体不需要到达物理边界",
        ],
        referenceSolution: `假设使用LOOK变体（到最远请求即反向）：

**SCAN(LOOK)算法：**
初始位置100，向增大方向：
- 向右服务：100 -> 150 -> 160 -> 184
- 反向服务：184 -> 90 -> 58 -> 55 -> 39 -> 38 -> 18

总寻道距离 = (184-100) + (184-18) = 84 + 166 = 250

**C-SCAN(C-LOOK)算法：**
初始位置100，向增大方向：
- 向右服务：100 -> 150 -> 160 -> 184
- 跳到最小请求处：184 -> 18
- 继续向右：18 -> 38 -> 39 -> 55 -> 58 -> 90

总寻道距离 = (184-100) + (184-18) + (90-18) = 84 + 166 + 72 = 322

注：如果到物理边界(如0和199)，距离会更大。`,
      },
    ],
    keyTakeaways: [
      "磁盘调度主要优化寻道时间（磁头移动距离）",
      "SSTF寻道距离最小但可能饥饿",
      "SCAN(电梯算法)无饥饿，是实际系统常用算法",
      "LOOK是SCAN的改进，不需要移动到磁盘边界",
      "C-SCAN提供更均匀的等待时间",
    ],
    relatedLessons: ["file-structure"],
  },
  {
    id: "file-implementation",
    title: "文件系统实现",
    brief: "文件物理结构的实现方式与磁盘空闲空间管理方法",
    analogy: "文件物理结构像存放行李的方式——连续分配是把所有箱子排成一排放在一个区域（快但不灵活），链接分配是每个箱子上贴着下一个箱子的位置（灵活但找第N个箱子要从头数），索引分配是有一张清单记录每个箱子在哪（快且灵活但清单本身占空间）。空闲空间管理就是仓库管理员记录哪些货架是空的。",
    prerequisites: ["文件系统基础", "磁盘的基本结构"],
    commonMistakes: [
      "连续分配的文件不能动态增长——因为后面的空间可能已被占用",
      "链接分配的FAT表是整个磁盘共享一张——不是每个文件一张",
      "索引分配的索引块本身也占一个磁盘块——小文件可能索引块比数据还大",
      "位示图中1表示已分配还是空闲取决于具体实现——做题看题目约定",
      "成组链接法是Unix/Linux使用的方法——不是简单的空闲链表",
    ],
    memoryAids: [
      "物理结构：连续（快+碎片）、链接（无碎片+慢）、索引（快+费空间）",
      "FAT = 文件分配表 = 整个磁盘的链接信息集中存放（链接分配的改进）",
      "空闲管理：空闲表（连续）、空闲链表（逐块/逐区）、位示图（紧凑）、成组链接（Unix）",
      "位示图：第i个字的第j位对应块号 = i×字长+j",
    ],
    keyTakeaways: [
      "连续分配：顺序访问和随机访问都快，但有外部碎片且文件不能动态增长",
      "链接分配：无外部碎片，但只能顺序访问（FAT改进后可随机访问）",
      "索引分配：支持随机访问且无外部碎片，但索引块占额外空间",
      "大文件索引：链接索引、多级索引、混合索引（Unix inode）",
      "空闲空间管理：位示图最常用，成组链接法适合大型文件系统",
    ],
    relatedLessons: ["file-structure", "disk-scheduling"],
    sections: [
      {
        id: "physical-allocation",
        title: "文件物理结构（磁盘分配方式）",
        type: "concept",
        content: `## 1. 连续分配

文件占用磁盘上一组**连续的块**。

目录项记录：起始块号 + 文件长度

| 优点 | 缺点 |
|------|------|
| 顺序访问快（磁头不用移动） | 外部碎片 |
| 支持随机访问（起始块+偏移） | 文件不能动态增长 |
| 实现简单 | 需要预知文件大小 |

## 2. 链接分配

每个磁盘块中存放指向下一块的指针。

### 隐式链接

目录项记录：起始块号 + 结束块号

- 优点：无外部碎片，文件可动态增长
- 缺点：只能顺序访问（找第i块要从头遍历），指针占空间，可靠性差

### 显式链接（FAT）

将所有块的链接指针集中存放在**文件分配表(FAT)**中。

- FAT常驻内存，查找不需要访问磁盘
- 支持随机访问（在内存中沿FAT链查找）
- Windows的FAT16/FAT32就是这种方式

## 3. 索引分配

为每个文件建立一个**索引块**，记录文件各块的磁盘块号。

目录项记录：索引块的块号

- 优点：支持随机访问，无外部碎片
- 缺点：索引块占额外空间（小文件浪费）

### 大文件的索引组织

- **链接索引**：多个索引块用指针串起来
- **多级索引**：索引块指向下级索引块（类似多级页表）
- **混合索引（Unix inode）**：直接块 + 一级间接 + 二级间接 + 三级间接`,
      },
      {
        id: "free-space",
        title: "空闲空间管理",
        type: "detail",
        content: `## 1. 空闲表法

用一张表记录每个连续空闲区的起始块号和长度。

| 起始块号 | 空闲块数 |
|----------|----------|
| 5 | 3 |
| 15 | 8 |

适合连续分配方式，分配和回收类似动态分区分配（首次适应等）。

## 2. 空闲链表法

### 空闲盘块链

将所有空闲块用指针串成链表。分配时从链头取，回收时挂到链尾。

- 缺点：分配多个块时需要多次操作

### 空闲盘区链

将连续的空闲块组成一个空闲区，各空闲区用指针串起来。

- 每个节点记录：起始块号 + 空闲块数 + 下一区指针
- 分配连续空间更高效

## 3. 位示图法（最常用）

用一个二进制位表示一个磁盘块的状态（0=空闲，1=已分配，或反之）。

磁盘有n个块 → 位示图需要 n 位 = ⌈n/8⌉ 字节

**块号与位示图的对应关系**：
- 块号 b 对应位示图的第 ⌊b/字长⌋ 个字的第 b mod 字长 位

优点：占空间小，容易找到连续空闲块（位运算）

## 4. 成组链接法（Unix/Linux）

将空闲块分组管理：
- 超级块中记录第一组空闲块（如100块）的信息
- 每组的第一个块记录下一组空闲块的信息
- 形成链式结构

分配时从当前组取，当前组用完则从下一组的信息块获取新一组。

适合大型文件系统，兼顾效率和空间。`,
      },
      {
        id: "unix-inode",
        title: "Unix混合索引（考研重点）",
        type: "detail",
        content: `## Unix inode结构

Unix文件系统的inode中包含13个地址项：

- **直接块**（前10个）：直接指向数据块
- **一级间接块**（第11个）：指向一个索引块，索引块中存放数据块地址
- **二级间接块**（第12个）：指向索引块，索引块再指向索引块
- **三级间接块**（第13个）：三层间接

## 计算文件最大长度

设磁盘块大小为1KB，地址占4字节：

每个索引块可存放地址数 = 1024/4 = 256个

- 直接块：10 × 1KB = 10KB
- 一级间接：256 × 1KB = 256KB
- 二级间接：256 × 256 × 1KB = 64MB
- 三级间接：256³ × 1KB = 16GB

文件最大长度 ≈ 10KB + 256KB + 64MB + 16GB ≈ 16GB

## 访问某字节需要的磁盘访问次数

访问文件第N个字节：
- 在直接块范围内：1次（直接读数据块）
- 在一级间接范围内：2次（读索引块 + 读数据块）
- 在二级间接范围内：3次（读两级索引块 + 读数据块）
- 在三级间接范围内：4次`,
      },
    ],
    exercises: [
      {
        id: "file-impl-ex1",
        title: "混合索引计算",
        description: "某Unix文件系统，磁盘块大小4KB，地址占4字节。inode中有8个直接块地址、1个一级间接、1个二级间接。求：(1) 一个索引块能存放多少个地址 (2) 文件最大长度 (3) 访问文件偏移量为40000字节处的数据需要几次磁盘IO（不考虑inode本身的读取）",
        difficulty: "hard" as const,
        hints: ["每个索引块的地址数 = 块大小/地址大小", "40000字节在哪个范围内？", "直接块范围 = 8×4KB = 32KB"],
        referenceSolution: "(1) 每个索引块地址数 = 4KB/4B = 1024个\n\n(2) 文件最大长度：\n- 直接块：8 × 4KB = 32KB\n- 一级间接：1024 × 4KB = 4MB\n- 二级间接：1024 × 1024 × 4KB = 4GB\n- 最大 = 32KB + 4MB + 4GB ≈ 4GB\n\n(3) 偏移40000字节：\n- 直接块范围：0~32767（32KB=32768字节）\n- 40000 > 32768，在一级间接范围内\n- 需要：读一级间接索引块(1次) + 读数据块(1次) = 2次磁盘IO",
      },
    ],
  },
];
